# 工作记录

> 项目：个人论文阅读 App（计划书 v1.0）
> 最近更新：2026-08-05（M7 iOS 选词根因修复：fable 5 agent 对抗审查发现官方 Safari 专用 endOfContent 重定位 JS 缺失，移植 #enableGlobalSelectionListener Safari 分支 + reset 监听，沙箱 27/27；待真机验证 iOS 选词）

## 项目目标

给自己用的论文阅读 Web App：单文件 `index.html` + 本地部署 pdf.js，托管 GitHub Pages，iPhone Safari"添加到主屏幕"当 PWA，Mac 浏览器同地址可用。后端 Supabase（Postgres + Auth + Storage）做双端同步，无自建服务器。中文界面、深色护眼风。二合一：阅读队列+笔记跟踪器 / 内置 PDF 阅读器。

## 里程碑进度

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M0 风险验证 | 已完成 | arXiv CORS（PDF 直连 / API 需代理）+ Supabase 连通通过；2026-08-02 |
| M1 骨架 | 已完成 | PWA 壳、三 tab 首页、条目 CRUD（IndexedDB）；2026-08-02 |
| M2 阅读器 | 已完成 | pdf.js v6 vendored、连续滚动渲染、进度记忆；2026-08-02 |
| M3 导入 | 已完成 | arXiv 元数据（Edge Function 代理）/ 本地 PDF / URL 导入；2026-08-03 |
| M4 笔记与高亮 | 已完成 | 条目笔记 + 页内高亮批注（双色 textLayer 偏移锚定）+ 批注汇总跳转；实机验证 39/39，修 Bug A/B/C（textLayer CSS / render 守卫 / 跨 span 分段）；2026-08-03 |
| M5 日推 | 已完成 | 订阅管理 + 今日推荐 tab（arXiv 分类拉取/打分/过滤/去重/当日缓存 + 加入想读/跳过）；实机 17/17（mock）+ 真 arXiv 端到端 19/19（部署后），C1-C5 全绿；2026-08-04 |
| M6 同步 | 已完成 | Supabase 登录 + 四表双向同步(LWW+_dirty+tombstone) + Storage PDF；C5 全绿(unit27+mock21+e2e22)；真机同步验证通过；2026-08-04/05 |
| M7 打磨 | 进行中 | 深色主题「墨纸」+ 烫金书页图标 + SW 更新提示 + 捏合缩放/平移 + 选区整页修复 + iOS textLayer 命中修复（tabIndex=0 + 删 callout:none + touch passive:true + touch-action + Safari endOfContent 重定位 JS，沙箱 27/27）；iOS 选词真机验证待用户；2026-08-05 |
| M8 部署 | 待开始 | GitHub Pages 上线、iPhone 主屏幕验收 |

## M0 当前进展

- [x] git init + `.gitignore`（2026-08-02）
- [x] CORS 实测页 `验证/m0-cors-test.html`（2026-08-02）
- [x] 用户本地跑 CORS 测试页，收集结果（2026-08-02，初步 n=1，见下方实测结果）
- [x] Supabase 连通性测试页 `验证/m0-supabase-test.html`（2026-08-02）
- [x] Supabase 连通性实测（2026-08-02，主模型 WebFetch 带 apikey 参数自测，通过）
- [x] 记录 M0 结论、commit

### M0 验证项

1. **arXiv PDF CORS（最大风险）**：浏览器能否直接 fetch `https://arxiv.org/pdf/<id>` 并读取字节供 pdf.js 渲染。失败回退候选：(a) 存链接跳系统浏览器阅读，(b) Cloudflare Worker 轻量代理。
2. **arXiv API**：`https://export.arxiv.org/api/query` 拉元数据（M3 导入、M5 日推依赖）。
3. **Supabase 连通性**：用纯 fetch 打 /auth/v1/health 与 /rest/v1/（M0 不装 SDK），验 URL+anon key+RLS。

### M0 CORS 实测结论（2026-08-02，已定）

用户在 Mac Chrome 跑 `验证/m0-cors-test.html`，两次 origin 均为 `file://`（未起 localhost 服务器）。虽 origin 为 file://，但**测试2 对照**使结论仍确定（推理见下）：

- **测试2 arXiv PDF（cors）：PASS**（status 200 / type=cors / 读到 2.16MB `application/pdf`）。从 file:// null 源能读到，说明 arxiv.org PDF 发 `Access-Control-Allow-Origin: *`。**=> PDF 可直连，pdf.js 无需代理。** 计划书 §7 原 PDF CORS 风险解除。
- **测试1 arXiv API（cors）：FAIL**（TypeError: Failed to fetch）。推理：测试2 证明 Chrome 允许 file://→https（前提：服务器发 ACAO:*），故测试1 失败不是 Chrome 的 file:// 策略，而是 export.arxiv.org 不发 ACAO:*。**=> API 跨域被阻，app（github.io）也无法直连，M3/M5 需轻量代理。**
- 测试3（no-cors）：OPAQUE，符合预期。

**为何 file:// 仍可下结论**：测试2 成功 ⟹ 服务器发了 ACAO:* 才能让 null 源读到；测试1 失败 ⟹ 该服务器没发 ACAO:*。两者对照排除了"Chrome file:// 策略"这一混淆因素。localhost 重测非必需（结论已定）；若日后想金标准复核可再跑。

**结论（定稿）**：
- PDF 加载策略 = 直接 fetch，无需代理。
- API 元数据策略 = 需代理（Cloudflare Worker 或 Supabase Edge Function，M3 时定）。

### M0 Supabase 连通性实测结论（2026-08-02，已定）

主模型用 WebFetch 带 `?apikey=` 查询参数自测（Supabase 网关接受 apikey 作 URL 参数，401 hint 已证实；curl/Python 受限不用）。项目：`https://hjrrocbisrgtxgqnflpi.supabase.co`（独立新建，与用户原项目隔离）。

- **测试1 Auth 健康（/auth/v1/health?apikey=）**：`HTTP 200`，`{"name":"GoTrue","version":"v2.194.0",...}`。**=> publishable key 有效，auth 连通。**
- **测试2 REST 根（/rest/v1/?apikey=）**：`HTTP 401`，`"Secret API key required"`。**=> 新版 key 系统下 /rest/v1/ 根（OpenAPI）只允许 secret key，publishable 不可用。不影响 app（只查具体表）。M6 调试注意：看 OpenAPI 得用 secret key 或 dashboard。**
- **测试3 查不存在的表（/rest/v1/__m0_probe__?apikey=）**：`HTTP 404`，PostgREST `PGRST205 "Could not find the table 'public.__m0_probe__' in the schema cache"`。**=> publishable key 能到 PostgREST、路由+鉴权通（新项目无表，故 404，符合预期）。**

**结论（定稿）**：
- Supabase 连通性 = 通过。publishable key（`sb_publishable_` 新格式）对表级 REST 查询有效，app 可用。
- 新项目无表无 RLS；建表 + RLS 策略留 M6。
- 新发现：新版 key 系统下 `/rest/v1/` 根端点要 secret key（publishable 不行）--M6 调试别踩坑。

## M1 当前进展

- [x] PWA 壳：`manifest.webmanifest`（占位配色 #1a1a2e、三尺寸图标 any+maskable、apple-touch-icon）（2026-08-02）
- [x] Service Worker `sw.js`：缓存 app 壳，cache-first + stale-while-revalidate（同源 GET），更新提示留 M7（2026-08-02）
- [x] 占位图标 `icons/{icon-192,icon-512,apple-touch-icon}.png`（PIL 生成，#1a1a2e 底 + #6c8cff 页面 glyph，haiku 视觉确认）（2026-08-02）
- [x] `index.html`：三 tab（今日推荐占位 / 阅读队列 / 已读归档）+ 条目 CRUD UI + IndexedDB papers store，原生 JS 单文件（2026-08-02）
- [x] 自测：node --check JS 语法、manifest JSON、DOM id 一致性、纯逻辑 20 断言、fake-indexeddb CRUD 10 断言、jsdom DOM 集成 20 断言（全绿）（2026-08-02）

### M1 数据模型（计划书 §4.1 子集）

papers store（keyPath=id，索引 status / updated_at）：id、title、authors[]、abstract、source_type（manual/url；arxiv 留 M3）、arxiv_id、url、local_file_path、status（want/reading/done/abandoned，全可逆）、rating（1-5 或 null）、tags[]、notes_md、read_progress（0-1，M2 写）、added_at、updated_at。

### M1 自测结论（2026-08-02，已定）

主模型自服务测试（不依赖用户浏览器）：

- **JS 语法**：`node --check` 抽取的内联脚本通过。
- **manifest**：JSON 合法。
- **id 一致性**：脚本里所有 `getElementById` 的 id 在 HTML 中均存在，无错配。
- **纯逻辑（vm 沙箱跑真实代码，20/20）**：stars、escapeHtml（防 XSS）、parseList、safeUrl（拒绝 javascript: 等）、statusActions（状态全可逆）、sortPapers（reading>want>done>abandoned，同状态按 updated_at 倒序）、uid。
- **IndexedDB CRUD（fake-indexeddb，10/10）**：openDB 建库建 store、put 新增/更新、getAll、delete、字段保留（rating/read_progress）、重开持久。
- **DOM 集成（jsdom + fake-indexeddb，20/20）**：灌数据→render→卡片数/排序/badge/星级/标签/URL 安全链接断言；模拟点"设为已读"→卡片移入归档 + DB 状态变更；模拟 FAB→编辑器→填表保存→DB+1 + 队列刷新。

**未自测（须真浏览器/真机，按计划属 M7/M8）**：SW 实际注册与缓存行为、Safari"添加到主屏"PWA 安装、真机视觉与手势。SW 代码为标准 cache-first 且 `if('serviceWorker' in navigator)` 守卫，风险低。

### M1 占位美学 token（M7 定稿）

背景 #1a1a2e、surface #242440、文字 #d4d4e0、muted #8a8aa3、accent #6c8cff、danger #e06c6c。图标同色系。这些是临时值，M7 按计划书 §5.3 定稿深色护眼主题时统一调整。

## M2 当前进展

- [x] Vendor pdf.js v6.2.108：`npm install pdfjs-dist` 后拷 `build/pdf.min.mjs` + `pdf.worker.min.mjs` + `wasm/`（jbig2/openjpeg/qcms/quickjs）+ `standard_fonts/` 到 `vendor/pdfjs/`（3.4MB，§5.1/§5.2 不走 CDN）（2026-08-02）
- [x] `sw.js` 预缓存两个 pdf.js JS（CACHE 升 v2）；wasm/字体走运行时 stale-while-revalidate 缓存（2026-08-02）
- [x] `index.html` 阅读器：卡片"阅读"按钮（有 url 才显示）；全屏 overlay（顶栏返回/页码 x/总/进度条/缩放 +/−）；pdf.js 动态 `import()`；连续滚动画布 + IntersectionObserver 懒渲染；适配宽度 baseScale + DPR 高清；arXiv abs/pdf 链接归一化为 `/pdf/<id>`（含老式 id 与版本号）；滚动防抖把 read_progress(0-1 分数) 写回 IndexedDB，打开时按分数恢复；缩放改 zoom 倍数重渲染（捏合手势精修留 M7）（2026-08-02）
- [x] 自测全绿（2026-08-02）

### M2 pdf.js v6 配置要点（M4 高亮复用）

- 浏览器用现代构建 `build/pdf.min.mjs`；Node 测试用 `legacy/build/`（现代构建依赖浏览器 API，Node 报"请用 legacy"）。
- `GlobalWorkerOptions.workerSrc = './vendor/pdfjs/pdf.worker.min.mjs'`；`getDocument({ url, wasmUrl: './vendor/pdfjs/wasm/', standardFontDataUrl: './vendor/pdfjs/standard_fonts/' })`。`useWorkerFetch` 浏览器默认 true（worker 自取 wasm/字体）。
- v6 清理用 `loadingTask.destroy()`（`PDFDocumentProxy.destroy` 在 v6 已不存在）。
- 渲染高清：`getViewport({scale: zoom*baseScale*dpr})` 设 canvas.width/height，CSS 尺寸用 `getViewport({scale: zoom*baseScale})`。

### M2 自测结论（2026-08-02，已定）

主模型自服务测试（不依赖用户浏览器），合计约 58 断言全绿：

- **JS 语法**：`node --check` 抽取内联脚本通过。
- **纯逻辑（vm 沙箱，15/15）**：M1 的 7 项 + 新增 `resolvePdfUrl`（arXiv abs/pdf/老式 id/版本号归一化、非 arXiv 直通、无 url 返回 null）。
- **pdf.js v6 真实解析（Node + legacy 构建，通过）**：pdf-lib 造 3 页测试 PDF -> getDocument -> numPages=3、getPage、getViewport、getTextContent 全通，验证 vendored 文件有效 + wasmUrl/standardFontDataUrl 配置正确。
- **阅读器集成（jsdom + fake-indexeddb + mock pdfjs，13/13）**：阅读按钮条件渲染（有/无 url）、overlay 开关、3 页画布布局、初始页码 1/3、滚动后 3/3、进度条更新、read_progress 防抖写回 + updated_at 更新、缩放失效重渲染、关闭销毁 loadingTask。（mock：把内联脚本里 `import('./vendor/pdfjs/pdf.min.mjs')` 替换成 `Promise.resolve(window.__mockPdfjs)`，canvas getContext/getBoundingClientRect 桩掉，其余代码原样。）
- **M1 回归**：M1 CRUD 10/10、M1 DOM 20/20 仍全过，无回归。

**未自测（须真浏览器，M8 验收）**：PDF 像素级 canvas 渲染（jsdom 无 canvas）、SW 实际预缓存 pdf.js、iOS 真机阅读器手势与滚动。pdf.js 解析链路已由 Node 测试覆盖，渲染逻辑（getViewport+render 调用）与 jsdom 集成已验证接线，真机只剩"画到屏幕上"这一步。

## M3 当前进展

- [x] arXiv API 代理：Supabase Edge Function `supabase/functions/arxiv-proxy/index.ts`（Deno，`verify_jwt=false`；转发 export.arxiv.org 并加 CORS *，M0 实测该 API 浏览器直连被 CORS 阻）（2026-08-03）
- [x] `supabase/config.toml`：`[functions.arxiv-proxy] verify_jwt = false`（2026-08-03）
- [x] `index.html` arXiv 一键导入：来源选「arXiv」显示链接/ID 输入 + 拉取按钮；`fetchArxiv` 经代理拉 Atom XML -> `parseArxivXml`（DOMParser + getElementsByTagNameNS，Atom 默认命名空间）解析 title/authors/abstract/url(abs)/tags(category term)；`extractArxivId` 兼容 abs/pdf 链接、纯 ID、老式 cs.LG/0701001、版本号；arxiv_id 落库（2026-08-03）
- [x] `index.html` 本地 PDF 导入：来源选「本地 PDF 文件」显示 file input；`DB_VERSION` 1->2（onupgradeneeded 加 `pdfFiles` store，保留 papers 不动）；选中文件读 ArrayBuffer 暂存 `currentFileData`，标题空时用文件名预填，60MB 软上限；save 时 `dbPutFile({id, data, size, added_at})`（key=paper.id）（2026-08-03）
- [x] `index.html` 阅读器 local 分支：`openReader` 对 `source_type==='local'` 走 `dbGetFile -> getDocument({data})`，其它走 `resolvePdfUrl -> getDocument({url})`；`canRead(p)` 控制「阅读」按钮（local 或 http(s) PDF 链接才显示，挡 javascript:）（2026-08-03）
- [x] 孤儿清理：编辑时来源从 local 改其它 -> `dbDeleteFile`；`del` 同时删 papers + pdfFiles（2026-08-03）
- [x] 自测全绿（2026-08-03）

### M3 数据模型补丁（计划书 §4.1）

papers 新增填写：`source_type` 增加 `arxiv`/`local` 两个值（原 manual/url）；`arxiv_id` 由 arXiv 拉取填入；`local_file_path` 对 local 条目置 `'local'` 标记（实际二进制在 pdfFiles）。新增 `pdfFiles` store（keyPath=id，value `{id, data: ArrayBuffer, size, added_at}`），key 与 papers.id 对齐，M6 同步时改走 Supabase Storage。

### M3 arXiv 代理方案（M0 遗留，已定）

用户在 CF Worker vs Supabase Edge Function 中选后者（与后端同生态、无需额外账号）。Edge Function `arxiv-proxy`：`GET ?id_list=<id>` -> 服务端 fetch `export.arxiv.org/api/query?id_list=<id>&max_results=1` -> 原样返回 `application/atom+xml` + CORS *。`verify_jwt=false`（arXiv API 本就公开，前端 M6 登录前也能调）。

**部署状态：代码就绪，待用户部署。** 部署方式（二选一）：
- CLI：`supabase functions deploy arxiv-proxy --no-verify-jwt`（需 supabase CLI 登录 + link 项目 hjrrocbisrgtxgqnflpi）
- Dashboard：项目 -> Edge Functions -> New Function -> 粘贴 index.ts -> Deploy
部署后前端 `ARXIV_PROXY = https://hjrrocbisrgtxgqnflpi.supabase.co/functions/v1/arxiv-proxy` 即可用。M3 自测已用 vm 沙箱跑真实 index.ts 验证转发/CORS/错误路径，但**端到端（前端->线上 Edge Function->arXiv）须用户部署后在浏览器验一次**。

### M3 自测结论（2026-08-03，已定）

主模型自服务测试（不依赖用户浏览器），合计 77 断言全绿：

- **JS 语法**：`node --check` 抽取内联脚本通过。
- **parseArxivXml（真实 Atom XML，WebFetch 从 export.arxiv.org 拉 1706.03762 作 fixture）**：arxiv_id 去版本号、title、authors[]、url=abs、abstract、tags（从 `<category term>`，不含 arxiv:primary_category）全对；坏 XML / 无 entry 抛错。
- **extractArxivId**：abs/pdf 链接、纯 ID、老式 cs.LG/0701001、带版本号、无效/空返回 null，7/7。
- **canRead**：arxiv/url/local 可读，manual 无 url / javascript: 不可读。
- **M1/M2 逻辑回归**：resolvePdfUrl（abs->pdf/pdf 直通/老式归一化/非 arxiv 直通/null）、escapeHtml、safeUrl、parseList、stars、sortPapers、statusActions 全过。
- **来源字段切换**：source=arxiv 显示 arxiv 区、local 显示 local 区、manual 全隐藏。
- **arXiv 一键拉取（jsdom + mock fetch 返回真实 XML）**：调代理 URL 带 id_list、title/authors/url/abstract/tags 回填、存盘后 arxiv_id 落库。
- **本地 PDF 导入**：file change 读 ArrayBuffer + 标题用文件名预填、save 后 papers 有条目（source_type=local, local_file_path='local'）、pdfFiles 有记录（data=ArrayBuffer, size 对）。
- **openReader local 分支**：`getDocument({data})` 加载、不传 url、wasmUrl 仍带、overlay 打开；**url 条目回归**：`getDocument({url})`。
- **删除**：del 同时清 papers + pdfFiles；**孤儿清理**：编辑 local->url 后旧 pdfFile 删除。
- **DB v2**：pdfFiles store 可写读删（v2 升级成功）、papers store 未破坏。
- **渲染**：local 条目显示「阅读」按钮、manual 无 url 不显示。
- **arxiv-proxy Edge Function（vm 沙箱跑真实 index.ts，Deno.serve 桩接）**：OPTIONS 204+CORS*、缺 id_list 400、POST 405、GET 200 转发原文+atom+xml Content-Type、上游 URL `export.arxiv.org/api/query?id_list=...&max_results=1`、User-Agent 带、上游异常 502。

**未自测（须真浏览器/真机）**：(1) 前端->线上 Edge Function->arXiv 端到端（待用户部署）；(2) 本地大 PDF 在 iPhone IndexedDB 实际占用与读取（M8 真机验收）；(3) file:// 直读 PDF 二进制（PWA 走 https，不涉及）。解析/导入/阅读器接线逻辑已由 node+jsdom 覆盖。

## M4 当前进展

- [x] annotations IndexedDB store：DB_VERSION 2->3，onupgradeneeded 建 `annotations`（keyPath=id，索引 `paper_id`）；annoStore + dbPutAnno/dbGetAnnosByPaper（走索引）/dbDeleteAnno/dbDeleteAnnosByPaper（2026-08-03）
- [x] 阅读器 textLayer 渲染：每页 `.textLayer` 容器盖在 canvas 上；renderPage 在 canvas 渲染后按 cssScale 懒渲染 textLayer（`new pdfjsLib.TextLayer({textContentSource,container,viewport,textDivs:[]}).render()`），textContent 缓存避免缩放重取；readerZoom 同时失效 `_tlScale` 触发重建（2026-08-03）
- [x] 选中文字->高亮/批注菜单：scrollEl 绑 mouseup（选中->showHlMenu 'new'，用 Range.toString().length 算容器文本偏移）+ click（点 mark.hl->'edit'，否则收起）；`#hl-menu` 双色 swatch（黄 #6c8cff 系外另取 rgba(255,204,64,0.45) / 蓝 rgba(108,140,255,0.45)）+ 批注输入 + 高亮/删除按钮（2026-08-03）
- [x] 高亮恢复 + 删除/编辑：applyHighlights 先 unwrap 旧 mark（insertBefore+normalize）再按 start_offset 排序重包；wrapTextRange 用 TreeWalker 定位文本节点 + surroundContents（跨节点回退 extractContents+insertNode）；saveHighlight/updateHighlight/deleteHighlight 写回 DB + 重画；closeReader 取消所有 textLayer（2026-08-03）
- [x] 阅读器笔记按钮 + modal 批注汇总：阅读器顶栏「笔记」按钮开关右下笔记面板（textarea 绑 papers.notes_md，保存写回 DB）；编辑器 modal 内 `#anno-list` 由 renderAnnoList 列出该论文全部批注（按页/偏移排序，60 字摘要素 + escapeHtml），点击跳阅读器对应页（openReader(paper,a.page) -> scrollToPage），每条带删按钮（2026-08-03）
- [x] del(paper) 一并清 annotations（避免孤儿）（2026-08-03）
- [x] canRead 收紧：非 local 需 http(s)（挡 javascript:）（2026-08-03）
- [x] 自测全绿（2026-08-03）

### M4 数据模型补丁（计划书 §4.2）

新增 `annotations` store（keyPath=id，索引 paper_id）：`{id, paper_id, page, text, start_offset, end_offset, color('yellow'|'blue'), note, created_at}`。选区定位用「textLayer 容器文本的字符偏移」：start/end 为容器 textContent 的 [start,end) 偏移（captureSelection 用 Range.toString().length 计算），text 为选中文本。恢复时按偏移用 TreeWalker 重定位文本节点包裹 `<mark class="hl">`。颜色双色（用户选「黄+蓝」），对应计划书 §3.5「可选颜色」；§6「单一强调色」的张力由"高亮单独允许多色、UI 强调色仍单一蓝"化解。

### M4 pdf.js v6 textLayer 要点（M8 真机复用）

- 构造：`new pdfjsLib.TextLayer({textContentSource: page.getTextContent()结果, container, viewport, textDivs: []})`；`await tl.render()` 后 `tl.textDivs` 是生成的 span 数组。`page.getTextContent()` 返回 `{items:[{str,transform,width,height,hasEOL,dir,fontName}], styles}`。
- v6 `TextLayer.render()` 需要真 canvas（jsdom 跑会 `TypeError: Cannot set properties of undefined (setting 'height')`）-> 自测用 mock TextLayer（按 items 造简单 span，DOM 结构与真实一致：span 内含文本节点，作为 container 直接子节点），只测选区->偏移->存->恢复的高亮逻辑；真 textLayer 像素定位渲染归 M8 真机。
- 清理：closeReader 对每个 textLayer 调 `layer.cancel()`。
- 缩放：readerZoom 把每页 `_tlScale=0` 失效，renderPage 重建 textLayer 后 applyHighlights 重包（偏移锚定不随缩放变，因为 textDivs 确定性重建）。

### M4 自测结论（2026-08-03，已定）

主模型自服务测试（jsdom + fake-indexeddb + mock pdfjs/TextLayer），合计 83 断言全绿：

- **JS 语法**：`node --check` 抽取内联脚本通过；manifest JSON 合法。
- **annotations CRUD（fake-indexeddb，6/6）**：dbPutAnno/dbGetAnnosByPaper（走 paper_id 索引）/dbDeleteAnno/dbDeleteAnnosByPaper，跨论文隔离、单删/全删、重开持久。
- **同页同 span 高亮（B1-B13，13/13）**：mock TextLayer 造 2 span -> renderPage 渲染 textLayer -> 选区 [5,15) -> mouseup 弹菜单 -> 存 yellow+note -> DB 落 anno（offsets/color/note/page/text 全对，text=slice(5,15)）-> DOM mark 文本/黄底/data-annoId -> 容器文本不变（偏移稳定）-> applyHighlights 重画仍 1 mark 文本对。
- **跨 span 高亮（C1-C9，9/9）**：选区跨 span1[20]+span2[5]（容器偏移 [20,30)）-> surroundContents 跨节点失败 -> extractContents+insertNode 回退成功 -> mark 文本=slice(20,30)、蓝底、容器文本不变。
- **恢复+编辑+删除（D1-D9，9/9）**：预置 2 条非重叠高亮 -> 重开阅读器 renderPage 自动画 2 mark -> 点 yellow mark 弹 edit 菜单（删按钮可见）-> 切蓝+改批注+更新 -> DB color=blue/note=updated -> 点 cross mark -> 删 -> DB 剩 1 条（被编辑的）、DOM 剩 1 mark。
- **阅读器笔记（E1-E5，5/5）**：toggle 开面板、textarea 预填 notes_md、保存写回 DB、面板收起。
- **批注汇总（F1-F6，6/6）**：renderAnnoList 按页排序、摘要素+escapeHtml、点条目跳阅读器（openReader(paper,page)）、每条删按钮 stopPropagation+dbDeleteAnno+重画。
- **del 清理（G1-G3，3/3）**：del(paper) 同删 papers + pdfFiles + annotations。
- **M3 本地阅读器回归（H1-H4，4/4）**：local 条目 getDocument({data}) 不传 url、阅读器开、renderPage 渲 textLayer 不崩。
- **逻辑回归（I1-I18，18/18）**：resolvePdfUrl（abs->pdf、保留版本号、url 直通、null）、canRead（local/arxiv/javascript:/manual）、extractArxivId（abs+版本保留 v、纯 ID、老式）、escapeHtml（&#39;）、safeUrl（返回 bool）、parseList、stars、sortPapers（reading>want>done>abandoned）、statusActions（全可逆 HTML 串）。
- **parseArxivXml（J1-J6，6/6）**：真实 Atom fixture（WebFetch 拉 1706.03762）-> arxiv_id 去版本、title、authors=3、abstract、url=abs、tags=2（category term）。
- **DB v3（K1-K4，4/4）**：三 store（papers/pdfFiles/annotations + paper_id 索引）均可写读、互不干扰、清理生效。

**未自测（须真浏览器/真机，M8 验收）**：(1) pdf.js v6 TextLayer 真像素渲染（jsdom 无 canvas，自测用 mock，只验高亮逻辑）；(2) 选区在真 textLayer（绝对定位 span）下的偏移准确性、iOS 长按选词手势；(3) 高亮 mark 在真机上的视觉与点击命中。偏移锚定逻辑、选区->存->恢复->编辑/删除链路、笔记、批注汇总已由 jsdom 覆盖。

### M4 实机验证（2026-08-03，提前到 M4 做）

用户指示把原计划 M8 的真机验证提前到 M4 阶段做（jsdom 自测用 mock TextLayer，盖不住真像素/真选区/真 mark 渲染）。在沙箱内搭了真浏览器管线：Playwright arm64 chromium（Chrome for Testing 无 linux-arm64 build）+ puppeteer-core + apt 下载的 X11 .so + `LD_LIBRARY_PATH`，http.server 与测试同一次 bash 跑（server 跨调用会死）。38->39 断言全绿、无 pageerror。抓到并修了 3 个 jsdom 测不出的 bug：

- **Bug A（textLayer 不可见，关键）**：pdf.js v6 bare `new TextLayer({...})` API 不注入 CSS（`adoptedStyleSheets` 为空），需宿主页提供官方 `.textLayer` 规则，把 pdf.js 设到每个 span 上的 `--font-height` / `--scale-x` 转成 `font-size` / `transform`。缺规则时 span 用默认 16px、无 transform，溢出容器被 `overflow:hidden` 裁掉，高亮 mark 不可见（修复前像素 diff 0）。补了取自 `pdfjs-dist@6.2.108 web/pdf_viewer.css` 的 `.textLayer` 块（`--text-scale-factor` / `--min-font-size` / 叶子 span 的 `font-size:calc(...)` / `transform:...scaleX(var(--scale-x))...` / `.markedContent{display:contents}`），并在 `renderTextLayer` 设 `--total-scale-factor`（= viewport.scale，calc 依赖，不设则容器塌 0×0 把 span 全裁）。修复后 span 缩到 261px、fontSize 11.47px、mark withinContainer、像素 diff 1617。
- **Bug B（关闭/重开竞态）**：closeReader 后 pending 的 renderPage 仍可能调 renderTextLayer，此时 readerState 已 null -> `Cannot read properties of null (reading 'textContentCache')`（3 次）。加 `if (!rs) return;` 守卫（与 applyHighlights 一致）。修复后无 PAGEERROR。
- **Bug C（跨 span 高亮不可见）**：wrapTextRange 跨节点时回退 `extractContents()+insertNode()`，把 mark 插成 `.textLayer` 直接子节点，拿不到 span 上的 `--font-height` / `--scale-x` -> font-size 塌 0、rect.width 0、视觉不可见（haiku 在 04/05 截图确认蓝色不可见，黄色单 span 可见）。重写为按文本节点分段：[start,end) 落在每个文本节点上的区间各自 `surroundContents` 一个 `<mark class="hl">`，同一 anno 的所有 mark 共享 `data-annoId` / 颜色 / 批注，每个 mark 留在各自 span 内继承正确 font-size/transform。applyHighlights / onReaderClick / update / delete 都按 annoId 找 anno + 全量重排（applyHighlights 先 unwrap 全部 mark 再按 anno 重包），天然兼容多 mark。修复后蓝色 mark `w=131,h=16` + `w=37,h=11`、`parentIsSpan=true`，haiku 确认 04/05 蓝色可见且重开恢复。

实机测试同时验证了：本地 PDF 导入（真 uploadFile + IndexedDB 二进制）、canvas 非空白（像素方差 std=28）、textLayer 12 span + `--total-scale-factor`、选中->菜单 new 模式、黄高亮落库、跨 span 蓝高亮、重开恢复、编辑改色+批注、删除（DB -1 + DOM mark 减）、笔记面板写回 DB、编辑器批注汇总 + 点击跳阅读器、删论文清 annotations/pdfFiles/papers 无孤儿、无 pageerror（favicon 404 已过滤）。

**测试基建**：`outputs/m4test/` 下 `generate-pdf.js`（pdf-lib 造带真文本层的 3 页测试 PDF）、`run-m4-test.js`（39 断言端到端）、`chrome-arm64/`（arm64 chromium）、`libs/`（X11 .so）、`shots/`（截图，haiku 视觉核验用）。harness §0.5：截图视觉 QA 必须走 haiku 子代理（主模型不能读图）。

**仍须真 iPhone（沙箱测不了，M7/M8 收尾）**：(1) iOS Safari 长按选词手势、选区手柄；(2) 捏合缩放手势（readerZoom 已实现，真机手感）；(3) Retina 像素渲染（沙箱 swiftshader 软渲染非真 GPU）；(4) PWA 添加到主屏 + 离线启动。偏移锚定、选区->存->恢复->编辑/删除全链路已由实机验证覆盖。

## M5 当前进展

- [x] C1 arxiv-proxy 扩展 `search_query` 透传：`supabase/functions/arxiv-proxy/index.ts` 在原 `?id_list`（M3 导入单篇）基础上加 `?search_query`（M5 按分类搜最新），`max_results` 软上限 200（app 用 100），`sortBy/submittedDate` + `sortOrder/descending`，缺参 400；thin CORS 转发不变（2026-08-04，commit 818d2c2）
- [x] C2 IndexedDB v4：`DB_VERSION` 3->4，onupgradeneeded 加 `subscriptions`（keyPath=id，`{id,name,keywords:[[组内OR]组间AND],categories:[cs.CL,…],enabled,created_at}`）+ `dismissed`（keyPath=arxiv_id）两 store，`if(!objectStoreNames.contains)` 守卫不破坏旧 store；helper dbPutSub/dbGetAllSubs/dbDelSub + dbPutDismiss/dbGetAllDismissed(返回 arxiv_id[])/dbIsDismissed（2026-08-04，commit 6b7c01d）
- [x] C3 设置页 + 订阅编辑器：header 加 ⚙ 按钮；`#settings-overlay` 底部弹层含 `#sub-list`（条目：名称/分类/关键词摘要 + 启用toggle/编辑/删）+ `#sub-editor`（名称、`#sub-cats` 分类多选 ARXIV_CATS 8 项、`#sub-keywords` 关键词组动态增删、`#sub-enabled` 启停、保存/取消）；saveSub 校验名称+≥1分类；事件委托 data-act（toggle/edit/del）+ kw-del（2026-08-04，commit 2806a11）
- [x] C4 拉取/打分/过滤/去重/缓存 + 今日推荐 tab + 想读/跳过（2026-08-04，commit add7031 + 312ee91）
- [x] C5 自测 + 实机验证（2026-08-04）

### M5 推荐设计（与用户对齐，已定）

- **来源**：仅 arXiv（Semantic Scholar / 其它留 v2）。
- **软出现**：`search_query` 只按分类（`cat:cs.CL OR cat:cs.LG`），关键词**不进 search_query**，降为 app 端打分信号——避免关键词把候选过滤没，扩大召回。
- **打分信号**：时效 recency（14 天线性衰减，>14 天 0）×1.0 + 关键词密度 keywordDensity（组内OR组间AND，命中组数/总组数；无关键词组则 0）×0.6 + 作者亲和 authorAffinity（命中用户已跟踪作者 1/0）×0.4。只排序不过滤。
- **topK = 15**，每订阅拉 100 篇，订阅间 ≥3s 限速（arXiv 礼貌使用），当日结果缓存到 localStorage（`m5-feed-YYYY-MM-DD`），缓存命中不重发 fetch。
- **去重**：跨订阅按 arxiv_id 去重（`seen` set）。**过滤**：已跳过（dismissed）/ 已在库（existingIds，按 arxiv_id）不进推荐；render 时再过一遍（缓存可能早于 want/skip 操作）。
- **加入想读**：复用 feed 已有元数据直接 `dbPut` status=want（不二次拉取），入库后 existingIds 过滤自动移出日推。**跳过**：`dbPutDismiss(arxiv_id)`，dismissed 过滤移出。两者只重渲染（用缓存），不重拉。

### M5 数据模型补丁（计划书 §4.3/§4.4）

新增 `subscriptions` store（keyPath=id）：`{id, name, keywords:[[t1,t2],[t3]]（组内OR组间AND）, categories:[cs.CL,…], enabled, created_at}`。新增 `dismissed` store（keyPath=arxiv_id）：`{arxiv_id, dismissed_at}`。M6 同步时这两表上 Supabase。

### M5 实机验证（2026-08-04，沙箱真浏览器）

复用 M4 的真浏览器管线（Playwright arm64 chromium + puppeteer-core + X11 .so + LD_LIBRARY_PATH）。**arxiv-proxy 的 search_query 扩展（C1）尚未部署**（线上仍是旧版，WebFetch 验证 `?search_query` 返回 400 "missing id_list"），故实机测试用 puppeteer `setRequestInterception` mock 代理响应（URL 含 arxiv-proxy + search_query -> 返回 canned Atom XML + `Access-Control-Allow-Origin:*` 镜像真代理 CORS 头；坑：mock 不带 CORS 头会被浏览器跨域拦截 `net::ERR_FAILED`）。17/17 全绿、无 pageerror：

- 空状态（无订阅）显示引导；设置页新增订阅（名称+2分类+2关键词组增删）+ 列表渲染；今日推荐 tab 拉取后 4 卡片，按推荐分降序（1.60/1.30/1.00/0.94，首卡=双组关键词命中+今日，末卡=单组命中+5天前）；首卡显示推荐分 badge + 命中关键词 tag + arXiv 链接；加入想读->日推-1 + 阅读队列出现该 want 论文；跳过->日推-1；刷新->重拉并过滤掉已入库/已跳过（剩 2）；proxy 命中 2 次（初次+刷新）。
- **haiku 视觉核验（harness §0.5）**：5 张截图全 PASS，深色主题、卡片布局、score/订阅/分类/关键词 badge、按钮层级均正常。修了一处 haiku 指出的计数陈旧：feed-status 之前停在"已更新 · 4 篇"而 want/skip 后卡片已减，改为 renderFeedFromCache 按 feedFreshness + 当前 visible.length 实时设置（commit 312ee91）。

### M5 自测结论（2026-08-04，已定）

jsdom + fake-indexeddb（C2/C3/C4 三套，合计 76 断言）+ 真浏览器 17 断言，全绿：

- **C2（13/13）**：subscriptions/dismissed CRUD、keywords AND/OR 结构保留、enabled 切换、dismiss 幂等、v4 升级不破坏 papers/annotations。
- **C3（21/21）**：设置页 UI——开/关、空状态、新增订阅（名称+2分类+2关键词组增删）、保存->DB、编辑（改名+加分类+停用）、toggle、删除、校验（名称空/未选分类阻止保存）、关闭。
- **C4（42/42）**：buildSearchQuery（软出现仅分类 OR）、scorePaper（recency/affinity/keywordDensity + 部分/无关键词）、parseArxivXmlList（多 entry + published）、fetchDailyFeed（跨 dup 去重、过滤在库/已跳过、打分排序、topK=15、localStorage 缓存、缓存命中不重发、no-subs 短路）、loadFeed 渲染、feedWant（入库 want + existingIds 移出日推）、feedSkip（dismissed 移出日推）。
- **实机（17/17）**：见上，端到端真 DOM + 真点击 + mock arXiv。

**未自测（须真机）**：(1) 跨天缓存失效（次日自动重拉，逻辑已实现但跨天场景未实跑）；(2) iPhone 真机手势/PWA 安装（留 M7/M8）。**部署 + 真 arXiv 端到端已于 2026-08-04 验收通过**：用户部署 arxiv-proxy 后，沙箱真浏览器用真实 arXiv 数据跑端到端 19/19（M3 导入 1706.03762「Attention Is All You Need」元数据回填+落库；M5 日推拉 cs.CL+cs.LG 最新 100 篇 -> 15 张真实卡片，首卡 GradCuit 推荐 1.62；加入想读->入队列+移出日推；跳过->移出日推；无 console error），haiku 视觉核验真长标题/长作者列表渲染 PASS。

## M6 同步（2026-08-04，已完成）

Supabase 登录 + 四表双向同步 + 本地 PDF 迁 Storage。**原生 fetch + publishable key** 直连 REST/Auth/Storage API（不 vendor SDK，计划书 §5.2 已改）。冲突策略 last-write-wins by `updated_at` + tombstone 软删除。同步时机：打开 app 自动 pull + 本地变更后 2s 防抖 push。

### C1 建表 + RLS（commit 62d89c1）

`supabase/migrations/0001_init.sql`：papers/annotations/subscriptions/dismissed 四表，`user_id` FK `auth.users` on delete cascade，`deleted_at` 软删，`updated_at`，CHECK 约束（source_type/status/rating）；RLS `user_id=auth.uid()`；storage bucket `pdfs`（私有）+ storage.objects RLS `split_part(name,'/',1)=auth.uid()`。Dashboard SQL Editor 运行。

### C2 邮箱+密码登录（commit e5aae9e）

设置页账号区：signup/login/logout + `sbSession`（localStorage 存 access_token/refresh_token/user）。`sbHeaders`（apikey + Authorization Bearer）。启动 + 登录钩子触发 syncPull。

### C3 同步引擎 pull/push/LWW（commit 35f5c78 + abbf4ab 修复）

`0002_fix_schema.sql`（BUG 1/2/5）：annotations drop `anchor jsonb` 加 `text/start_offset/end_offset`（对齐本地 M4 实现）；subscriptions 加 `created_at`；drop `touch_updated_at` trigger（trigger 在 UPDATE 时把 `updated_at` 改写为服务器 `now()`，破坏客户端按编辑时间定胜负的 LWW 语义；删后客户端 dbPut 设 `updated_at` 直达服务器）。

同步引擎核心：

- **`_dirty` boolean flag** 替代 lastSync 方案（dbPut/dbDelete 置 true，rawPut/pull/push-success 置 false，syncPush filter `_dirty===true`）--根治"pull 来的行因 `updated_at>lastSync` 被重推"和"本地未推改动被远端旧值 race 覆盖"两个隐患。
- syncPull：远端 `updated_at` > 本地则 rawPut（含 `deleted_at` 软删传播），拉来的 `_dirty=false` 不重推。
- syncPush：`on_conflict=id` upsert，payload 删 `_dirty` + 注入 `user_id` + `deleted_at=null` 补全；并发守卫 `pushing`；成功后 `_dirty=false`。
- `sbFetch` 包装：401 自动 `sbRefresh` 重试一次。
- dismissed：`on_conflict=user_id,arxiv_id` upsert。
- v5 迁移：legacy 对象补 `_dirty=true` + `updated_at`。

对抗性 opus 子代理审查发现 11 bug（2 fatal：annotations anchor vs text/start_offset/end_offset schema 不匹配、subscriptions 缺 created_at），abbf4ab 全修。

### C4 本地 PDF 迁 Storage（commit 60d400f）

`sbUploadPdf`/`sbDownloadPdf`/`sbDeletePdf`（bucket=pdfs，path=`<uid>/<paperId>.pdf`）。save() 导入时 dbPutFile（IDB 离线缓存）+ sbUploadPdf（同步等待，失败 alert 不阻塞保存）；openReader() IDB 优先，缺失 fallback sbDownloadPdf 下载 + 回填 IDB（跨设备读本地 PDF 的关键路径）；del()/来源切换删 Storage。sbUploadPdf 用 `data.slice(0)` 备份解决 ArrayBuffer transfer 后 401 重试。arXiv/url PDF 不上传。IDB pdfFiles 保留作离线缓存，Storage 是跨设备真相源。

### C5 自测（2026-08-04，全绿）

- **unit test 27/27**（`outputs/m6_unit_test.js`）：tsOf / LWW 覆盖决策（含 BUG4 本地未推改动不被远端旧值覆盖）/ _dirty filter / syncPush payload 构造（含 BUG1 anno schema）/ tombstone / 并发守卫 / 防抖。
- **真浏览器 mock 21/21**（`outputs/m4test/run-m6-test.js`，puppeteer + setRequestInterception mock supabase.co，带 ACAO:* 头）：登录 renderAcct / dbPut _dirty / schedulePush 防抖 2s / syncPush payload（无 _dirty + user_id + deleted_at=null）/ push 后 _dirty=false / syncPull 拉入 + _dirty=false / LWW 远端更旧不覆盖 / 软删 tombstone / dbAll 过滤 deleted_at / tombstone push / 无 JS 错误。
- **真实 Supabase e2e 22/22**（`outputs/m6_e2e.js`，随机 gmail signup，email confirm 关闭后直接拿 session）：papers push/pull/upsert/trigger 已删（updated_at 不被改写）；annotations schema 对齐（text/start_offset/end_offset，无 anchor）；subscriptions created_at 持久化；dismissed upsert；tombstone 软删传播；RLS anon 写被拒 + anon 读空数组；Storage 上传/下载/字节一致/RLS anon 被拒；cleanup 7 rows + 1 object。
- **haiku 视觉核验**：深色主题/中文/排版/登录状态/论文卡片/操作按钮均正常。

sw.js CACHE bump v5->v6。

### M6 真机验证（2026-08-05，commit d76d986）

手机+电脑同账号登录，四表双向同步 + Storage PDF 跨设备验证通过（用户确认"手机测同步没问题"）。验证中发现并修 3 个真机 bug：

- **IndexedDB 版本错误**（用户自行解决）：浏览器加载 M4 旧 app（Service Worker 缓存了旧 index.html，DB_VERSION=3），开不了已升级的 v5 DB -> "requested version (3) is less than the existing (4)" + db 为 null -> 保存报 "Cannot read properties of null (reading 'transaction')"。根因是旧 SW 缓存，解法：无痕窗口或注销旧 SW 重新加载最新 app。
- **email confirm 注册提示误导**：生产环境重开 email confirm 后，signup 不再返回 session，但旧代码仍提示"注册成功，已登录"并触发 syncPull（必然失败）。改为按 `data.access_token` 分支：有则直接登录 + syncPull，无则提示"验证邮件已发送，请到邮箱点验证链接后再点登录"。
- **iOS 选文本选不了**（关键）：阅读器只监听 `mouseup`，iOS Safari 选文本松手时系统接管触摸、mouseup 不触发，高亮菜单弹不出。改为监听 `document selectionchange` + 300ms 防抖（拖拽把手时持续触发，松手后选区稳定才弹菜单），鼠标/触摸通用。同时 sw.js CACHE v6->v7 触发 SW 更新清旧缓存。

**未自测（须真机）**：iOS 选文本修复待用户确认（改用 `?v=2` 绕 SW 缓存重测）、大 PDF 上传体验、iOS 捏合缩放手感、PWA 添加主屏 + 离线启动。留 M7/M8。

## M7 打磨（2026-08-05，进行中）

用户从 3 个配色方向（墨纸/青夜/精修当前）+ 3 个图标方向（烫金书页/折角页+高亮/P 印章）中选定「墨纸 + 烫金书页」。

### 配色 + 图标定稿（commit 2998a22）

- **配色 token**：bg #1a1814 暖墨黑 / surface #24211c / surface-2 #2e2a23 / text #e8e3d8 米白 / muted #8a8478 / accent #c9a96e 古铜金（替代原技术蓝 #6c8cff）/ accent-h #b8975a / border #322e26 / danger #c66b5a。灵感：旧书页反相 + 烫金题字，蓝光少护眼。
- **status badge 暖色化**：reading 用古铜金、done 绿暖化、abandoned 红暖化、want 暖褐。
- **高亮双色**：黄 rgba(232,196,106) / 蓝 rgba(111,168,212)；::selection 用古铜金 rgba(201,169,110)。
- **图标**：烫金书页（暖墨黑底 + 古铜金描边书页 + 8 横线文字，末行 2/3 宽），maskable 中心 80% 安全区内。haiku 视觉核验两轮：首版书页太大超安全区 + 横线太稀疏像列表 -> 调整（四边留 20%/18%、8 行加粗、末行 2/3 宽）后 PASS。
- manifest theme/background_color #1a1814；sw.js CACHE v7->v8。

### SW 更新提示（commit 1fcdfd5）

监听 navigator.serviceWorker.controllerchange，新 SW skipWaiting 接管后显示"有新版本，点击刷新"横幅（accent 古铜金底 + bg 暖墨黑字，点击 reload）。用 hadController 区分首次注册（不提示）vs 版本更新（显示横幅）。sw.js 已有 skipWaiting + claim，无需改。

### 阅读器捏合缩放手势（commit bc686fb）

readerZoom 加 targetZoom 参数（捏合直接设目标倍数，按钮仍用 delta 乘）。onReaderTouchStart/Move/End：双指算距离比 × 初始 zoom -> readerZoom，rAF 节流避免频繁重渲染。touchstart/touchmove passive:false，双指 preventDefault 阻止 iOS 浏览器页面缩放（单指 return 不影响滚动）。

### 捏合平移 + 选区整页修复（2026-08-05）

真机反馈两个 bug：(1) 双指缩放后不能平移，只能放大中间；(2) 长按选词选区跨整页 + 顶栏 UI（"笔记"按钮被选进去）。

**捏合平移**：根因 `.reader-scroll` `overflow-x:hidden` + page div 未设宽度 -> zoom>1 时 canvas 溢出被裁且无水平滚动。修：`overflow-x:auto` + `align-items:flex-start` + `.reader-page` `margin:0 auto`（zoom=1 居中/zoom>1 可滚）+ renderPage/readerZoom 同步设 `div.style.width=canvas` 宽度。

**选区整页**：根因 pdf.js v6 核心 `TextLayer.render` 不创建 `.endOfContent`（那是 web 层 TextLayerBuilder 的职责，bare API 没有）+ 缺 `-webkit-text-size-adjust:none`（iOS 放大透明文字 span 致 hit-test 失败）+ captureSelection 无跨容器守卫。修：(1) textLayer CSS 加 `-webkit-text-size-adjust:none;text-size-adjust:none;forced-color-adjust:none` + endOfContent 官方规则（user-select:none 屏障）；(2) renderTextLayer 手动 append endOfContent div；(3) onReaderSelectionChange 即时管理 `.selecting` 类（`range.intersectsNode`）；(4) captureSelection 守卫：选区起/终点不在该 pageEl 内或等于容器自身时返回 null（防 iOS 选整个 textLayer -> 整页高亮）。

**iOS 透明文字不可选（第二轮修复，2026-08-05）**：第一轮修完后真机反馈 iOS 仍完全无法选 PDF 文字--长按弹的是系统拷贝/翻译菜单而非高亮菜单，复制只能拿到顶栏"笔记"二字。根因（haiku 查 pdf.js 官方源码 + iOS WKTextInteraction 行为）：textLayer span 原用 `color:transparent` 隐藏透明文字，**iOS Safari 的 WKTextInteraction 长按 hit-test 会忽略 `color:transparent` 的文字**（视为无可选文本），回退到最近的可选可见文本（顶栏按钮）；桌面 Chromium 只看 DOM 文本节点 + user-select，不看 color，故沙箱测不出此问题。修：`color:transparent` -> `color:rgba(0,0,0,0.01)`（非 transparent 关键字，alpha 0.01 视觉不可见但 iOS 识别为可选文本）+ `-webkit-touch-callout:default` + leaf span `z-index:1`。**不用 `opacity:0.001`**（会让继承的 `mark.hl` 高亮也近乎不可见）；rgba 路线只动 color 不动 opacity，高亮不受影响。沙箱验证 mark.hl 背景 `rgba(232,196,106,0.45)` 仍可见。

**iOS textLayer 命中修复（第三轮，2026-08-05，真根因）**：第二轮 rgba 修复真机仍不行。派 sonnet agent 深挖 pdf.js GitHub issues + 官方 v6.2.108 源码（`web/text_layer_builder.js` / `text_layer_builder.css` / `src/display/text_layer.js`），并 WebFetch 核对官方源码确认。**核心结论：`color:transparent` 不是病根**（官方 pdf.js 就用 transparent 且 iOS 能选；iOS WKTextInteraction 对已布局文字字形做命中测试，与 color 无关）。真根因是 bare 核心 `TextLayer` API 缺 `TextLayerBuilder`（web 层）的关键设置，导致 iOS 不把 textLayer 当"可选文本目标"，回退到顶栏按钮。修复（全部对齐官方）：
- **JS `layoutPages`**：textLayer 容器加 `tabIndex=0`（复刻官方 `TextLayerBuilder` constructor 的 `this.div.tabIndex = 0`）--**iOS 长按选词命中需可聚焦文本容器，这是最关键一处**。
- **JS `renderTextLayer`**：`getTextContent()` -> `getTextContent({includeMarkedContent:true})`（对齐官方 `streamTextContent` 默认参数；先不加 `disableNormalization` 以免影响高亮偏移）。
- **CSS `.textLayer`**：`overflow:hidden` -> `clip`（hidden 建立 scroll container 改变 iOS 触摸派发）+ 补 `caret-color:CanvasText`（告知 iOS 文本选区上下文）+ `letter-spacing/word-spacing/color-scheme/transform-origin`。
- **CSS span**：`color:rgba(0,0,0,0.01)` 改回 `transparent`（与官方一致）+ `-webkit-touch-callout:default` -> `none`（抑制 iOS 系统拷贝/翻译菜单，只走本应用 selectionchange 高亮菜单；不影响文字选中与放大镜）。
- **CSS `.endOfContent`**：`z-index:-1` -> `0`（对齐官方）+ `.selecting` 删多余 `height:100%`/`z-index:-1`（官方仅 `top:0`）。
- 官方背景：pdf.js #17923 维护者明确 Safari mobile 选区扩展"still broken... out-of-scope"，#15691 关为 not_planned（Firefox 是首要目标），但官方 demo 初次长按能命中（只是跨行扩展有 bug）。

**沙箱真浏览器验证（23/23 PASS）**：新增 textLayer `tabIndex=0` + `overflow=clip` + `caret-color=CanvasText` + endOfContent `z-index=0` 四项检查全过；span `color=transparent`（对齐官方）；mark.hl 背景 `rgba(232,196,106,0.45)` 仍可见；选词 hlDraft 358-369（includeMarkedContent 未破坏偏移）；守卫 + 缩放平移不回归；无 JS 错误。haiku 视觉 QA 4 张截图 PASS。**iOS 真机命中验证待用户**（chromium 测不出 iOS WKTextInteraction 行为）。

**iOS 选整页修复（第四轮，2026-08-05，对比官方 demo）**：第三轮后真机反馈"对着文字长按直接选整页"。先请用户测官方 demo（mozilla.github.io/pdf.js/web/viewer.html）--官方完全能选单个词，确认非 pdf.js 通病而是我的实现差异。用 chromium dump 官方 demo textLayer 真实 DOM + computed style（diag-official.js），并 WebFetch 核对官方 v6.2.108 `text_layer_builder.js` + `text_layer.js` + `display_utils.js` 源码。逐字段对比找到元凶：**`-webkit-touch-callout:none`**（第三轮所设，官方未设/default）--iOS 上 callout:none 会让长按不触发正常"选词放大镜+选中单个词"，而是触发 select-all（选整页）。官方 `text_layer_builder.css` 没设此属性。修：删 `-webkit-touch-callout:none`（回 default，与官方一致）。同时修两处次要偏差对齐官方：`.textLayer` `z-index:2` -> `0` + `left:0;top:0` -> `inset:0`。`--total-scale-factor` 经源码核对：官方 `setLayerDimensions` 只用不设（由宿主页设），本应用在 renderTextLayer 手动设 cssScale 是对的，无需改。diag-mine.js 逐字段对比：tabIndex/overflow/caretColor/zIndex/eocZIndex/spanColor/spanZIndex/spanTransform 结构与官方完全一致，hitTestAtSpanCenter=SPAN。**iOS 真机验证待用户**（-webkit-touch-callout:default 是否消除选整页）。

**iOS 长按手势被抑制修复（第五轮，2026-08-05，passive:false 元凶）**：第四轮后真机仍不行。官方 demo 能选而我的不能，且 textLayer 结构逐字段一致，说明问题在 textLayer 之外。定位到 `scrollEl.addEventListener('touchstart'/'touchmove', ..., { passive: false })`（M7 捏合缩放所加）--**iOS Safari 对声明 `passive:false` 的元素会抑制系统长按选词手势**（担心被 preventDefault），导致选词放大镜根本不弹。官方 demo 无此类监听。修：(1) touchstart/touchmove 改 `{ passive: true }`；(2) 删 onReaderTouchStart/Move 的 `e.preventDefault()`（passive 下无效）；(3) `.reader-scroll` 加 `touch-action: pan-x pan-y`（允许双向滚动 + 禁止浏览器原生 pinch-zoom，但不禁止长按--pan-x pan-y 只控 pan/zoom 不控长按）。捏合缩放改为：passive 监听读双指距离 -> readerZoom，靠 touch-action 阻止浏览器原生缩放（不再靠 preventDefault）。沙箱 23/23 PASS（选词/守卫/缩放平移不回归）。**iOS 真机验证待用户**（passive:true 是否恢复长按选词放大镜）。

**iOS 选词根因修复（第六轮，2026-08-05，fable 5 agent 对抗审查）**：第五轮 passive:true 修复后真机仍不行，用户明确"根本不是这个问题"，要求开 5 个 fable 子 agent 做对抗性审查。5 agent 高度收敛于此前六轮都漏的同一处：**官方 `TextLayerBuilder.#enableGlobalSelectionListener` 的 Safari 专用 endOfContent 重定位 JS 逻辑只复刻了 CSS 那一半，JS 那一半完全没做**。WebFetch 官方 v6.2.108 `web/text_layer_builder.js` 确认：selectionchange 处理对非 Firefox/非 Chromium148+ 的浏览器（**iOS Safari 必走此分支**）会：把 endOfContent 从层底**挪到选区 anchor 旁** + 设 `width/height=层尺寸` + `userSelect:'text'`（覆盖 CSS 的 `user-select:none`），并在 pointerup/blur/keyup 时 reset（归位 + 清 inline + 移除 `.selecting`）。官方注释原文--"Safari 上指针悬到空白处时选区会扩展到当前选区与 .endOfContent 之间的全部文本"--与我的"长按直接选整页/选进顶栏笔记"完全吻合。我只做了 static 版：endOfContent 永远在层末尾 + `.selecting` 时 `top:0` 盖满整层 + `user-select:none`。后果链：iOS 长按/拖把手触点落 glyph 间隙 -> 命中盖满整层的 `user-select:none` 屏障 -> WebKit 把选区扩展到屏障 DOM 位置=整层末尾 -> 选整页；屏障 user-select:none 端点不能落内部 -> 端点吸附到屏障外最近可选节点 -> 逃出 textLayer 吃顶栏"笔记"。

修复（全部对齐官方，含 5 agent 点名的次要点）：
- **onReaderSelectionChange 重写**：加官方 activeTextLayers 逻辑 + **Safari 分支**（modifyStart 判定 + anchor 上溯 + `mark.hl` 类名映射官方 `highlight` + endOffset===0 回退遍历 + insertBefore 挪 endOfContent 到 anchor 旁 + 设 inline width/height/`userSelect:'text'` + prevRange 跟踪），try-catch 包裹防极端选区形态。
- **reset 监听**：新增 `resetReaderTextLayer`/`resetAllReaderTextLayers` + `onReaderPointerDown/Up/onReaderWinBlur/onReaderKeyUp`（复刻官方 pointerup/blur/keyup reset），openReader 注册 + closeReader 移除。
- **renderTextLayer**：endOfContent 引用存入 `rs.textLayers[pageNum].endOfContent`（reset 依赖）。
- **touchend 补 passive:true**（第五轮漏改，onReaderTouchEnd 只清 _pinching 从不 preventDefault）。
- **删 `-webkit-overflow-scrolling:touch`**（官方 viewerContainer 无、iOS 13+ 废弃、fable 点名引发子元素 hit-test/选区怪癖）。
- **captureSelection 加 endOfContent 端点守卫**：端点落 endOfContent（被 Safari 分支挪到 anchor 旁的 user-select:text 屏障）-> return null，防 setEnd 算出垃圾偏移 -> 整页高亮。
- **showHlMenu y 夹取**：`top = max(8, min(y+8, innerHeight - mh - 8))`，选区在屏下半部时不弹屏外。
- **touch-action:pan-x pan-y 暂保留**（pinch 缩放依赖它禁止浏览器原生 pinch-zoom），待实机验证 Safari 分支修复后再 A/B 是否需删（fable 建议改 gesturestart preventDefault）。
- 5 agent 另点名的结构层（.reader-scroll flex column / .reader-page flex + canvas static）暂不动（agent 自评"语义不变可安全应用"，但改 flex 影响布局有视觉风险，保持聚焦待 A/B）。

**沙箱真浏览器验证（27/27 PASS）**：新增 4 项检查全过--textLayers[1].endOfContent 引用存在且指向容器内 .endOfContent / 选词后 Safari 分支挪位 endOfContent（inline userSelect:"text"）/ 守卫选区 endContainer=endOfContent -> 菜单不弹 / pointerup 后 reset（endOfContent 归位 lastChild + 清 inline width/height + 移除 .selecting）；原有 23 项不回归（选词 hlDraft 358-369 / mark.hl 可见 / 守卫 / 缩放平移 / 无 JS 错误）。**iOS 真机验证待用户**（Safari 分支是否消除选整页+选进顶栏；chromium 测不出 iOS WKTextInteraction 行为，但官方注释直接背书此症状）。

**未自测（须真机）**：墨纸配色真机视觉 + 烫金书页图标在主屏显示、SW 更新提示横幅触发、捏合缩放/平移手感、textLayer 真机 Retina 像素调优、iOS 选文本修复确认、PWA 主屏安装 + 离线启动。

## M8 部署 + 两 bug 修复（2026-08-05）

**部署准备**：仓库已建（github.com/wyzqwq/paper-reading，SSH 推送，因 GitHub 2021 起禁密码认证）。子路径部署审计：所有相对路径（manifest start_url/scope `./`、SW 注册 `sw.js`、pdf.js `./vendor/`、apple-touch-icon）均兼容 `/paper-reading/` 子路径；加 `.nojekyll`（commit b7cf726）；计划书加 §11 v2 待办「翻译功能」（commit 28de538）；无 secret 入库（仅 sb_publishable key，public-safe）。打 annotated tag `v1.0`（保留里程碑叙事 + commit hash 引用 + bisect 价值，优于 squash）；起草 `release-notes-v1.0.md`（未入库，用户决定 commit 或删）。

**Bug 1：本地 PDF 保存后添加页不关（Mac）**：用户在 Mac 添加本地 PDF，点保存后论文实际已入库但添加页不关。根因：`save()` 在 `dbPut`+`dbPutFile` 后 `await sbUploadPdf`（Supabase Storage 上传），上传慢/卡/抛错时阻塞，永远到不了 `closeEditor()`。修：重组 `save()`--本地存盘后立即 `closeEditor()+render()`，把 `uploadFile/uploadId` 在置空前捕获，Storage 上传改后台非阻塞（`.then/.catch` 失败弹提示，不阻塞 UI）；`sbDeletePdf` 也改非阻塞。`sbUploadPdf` 加 `AbortController` 120s 超时（timeout/异常返回 false 而非挂起），401 仍走 refresh 重试。

**Bug 2：iOS 子路径 PWA 图标缺失（GitHub Pages）**：用户反馈 Mac「添加到 Dock」图标正常，iPhone Safari「添加到主屏」无图标；之前局域网根路径部署反而正常，GitHub Pages 子路径 `/paper-reading/` 才坏；网站 favicon 正常显示，仅 Add-to-PWA 图标缺。根因：iOS Safari 16.4+ 用 manifest icons 做「添加到主屏」，但把 manifest 内相对 icon 路径解析到**域名根**（非 manifest URL），子路径下 `icons/icon-192.png` -> `https://wyzqwq.github.io/icons/...` 404。macOS Safari「添加到 Dock」用 apple-touch-icon（HTML link，按页面目录解析），故 Mac 正常。修（三处，皆可移植兼容根路径与子路径）：
- **sw.js 拦截 manifest 请求**：fetch handler 拦 `/manifest.webmanifest` GET，拉真 manifest 后按 manifest 所在目录把 icon `src` 改写成绝对路径（`base + ic.src.replace(/^\.?\//,'')`），`base` 由 manifest URL 算出（根路径 `/` 或子路径 `/paper-reading/`），返回新 Response（`Content-Type: application/manifest+json`）。manifest URL 保持真实同源（不用 blob/data URL，iOS 才认 PWA）。start_url/scope 不动。
- **sw.js 离线回退细化**：非 navigation 资源请求失败返回 404 而非 index.html（防 iOS 拿 HTML 当图标，Add to Home Screen 显示空白）；navigation 仍回 index.html。
- **apple-touch-icon 运行时绝对化**：HTML 加 `sizes="180x180"`；JS 按 `location.pathname` 算 base 设 `apple-touch-icon.href` 为绝对路径（兼容 LAN 根与 GitHub Pages 子路径，Mac「添加到 Dock」也受益）。
- SW 版本 bump v19 -> v21。

**沙箱真浏览器验证（chromium，SW manifest 拦截 2/2 PASS）**：重新解压 m4test chromium + X11 libs，puppeteer 起 server 挂仓库到子路径 `/paper-reading/`（端口 8091）与根路径 `/`（端口 8092），加载页面等 `navigator.serviceWorker.ready` + `clients.claim`，再 `fetch(manifest)` 验证：子路径下 icon src 全部改写为 `/paper-reading/icons/icon-192.png` / `/paper-reading/icons/icon-512.png`（any + maskable），Content-Type `application/manifest+json`；根路径下改写为 `/icons/...`。start_url/scope 保持 `./`。页面无 JS 错误（apple-touch-icon JS + SW 注册干净）。`node --check` index.html 脚本 + sw.js 语法 OK。**iOS 真机验证待用户**（chromium 测不出 iOS「添加到主屏」是否经 SW 拉 manifest；但 manifest 在页面加载时即被 SW 改写，iOS 装机用的就是这份）。

**未自测（须真 iPhone）**：iOS「添加到主屏」图标显示、本地 PDF 保存后添加页关闭、Storage 后台上传成功。

## v2 翻译功能验证（2026-08-06）

v2 代码已分 5 commit 落地（6c635d5 数据层 IndexedDB v6 + translations/user_secrets store + sync 扩展；12deca2 设置页 DeepSeek key + user_secrets 同步；e82f413 引擎 阅读器开关 + streamTextContent 聚合 + 整篇预译循环 + source_hash 缓存；95ec401 叠层 句子偏移->span 映射 + 点句 ZH 浮层 + M4 共存；f19423a UI 收尾 tr-status 截断 + sw CACHE v22），依计划书 §11（grill-me 对齐 2026-08-06）。本节为**收尾验证**：沙箱 VM 重置、m4test 不在，从零重搭三层测试基建。

**DeepSeek 模型名核验（load-bearing）**：计划书/代码用 `deepseek-v4-flash`（非推理、无 thinking、快、便宜）。web 核验确认该模型名正确（OpenAI 兼容 API，base `https://api.deepseek.com/chat/completions`，response_format=json_object，temperature 0.3）。

**测试 1：jsdom + fake-indexeddb 单测（55/55 PASS）** — outputs/v2_unit_test.js。整脚本载入真 index.html，beforeParse 注入 fake-indexeddb + node webcrypto（sha256Hex 用 subtle.digest）+ fetch stub，url 设 localhost（非 opaque origin 才有 localStorage）。覆盖：sha256Hex、trFindOffset 三级匹配（原样/去\n/归一化空白）、trWrapSentence+trUnwrapPage（单 span/跨 span/end<=start）、数据层（dbPutTranslation 复用 id 更新 + source_hash 重译覆盖 + 同 page 多行取最新 + 软删过滤 + dbGetAllTranslations 去重 + user_secrets）、syncPush payload（translations on_conflict=id + _dirty 删除 + deleted_at=null + Prefer merge-duplicates；user_secrets on_conflict=user_id + 无 deleted_at；只推 dirty 行；推后 _dirty=false）。踩坑：fake-indexeddb require 返回 {indexedDB} 子键非工厂；jsdom window.crypto 无 subtle 需 Object.defineProperty 覆盖；let db 词法用 eval 读；rawPut 强制 _dirty=false 不能用来造 dirty 行。

**测试 2：translate-proxy Edge Function 沙箱测（48/48 PASS）** — outputs/v2_proxy_test.js。node 22 原生类型剥离动态 import 真 index.ts，stub Deno.serve/env + fetch（PostgREST + DeepSeek 路由）。覆盖：OPTIONS 204+CORS、GET 405、无 JWT 401、user_secrets 无 key 400、正常 200（pairs 数/en verbatim/zh 译文/DS model=v4-flash/response_format json_object/stream false/temperature 0.3/system prompt/user content=原文/PG 用 anon key + 调用者 JWT 非 service role）、DS 401 透传 key 无效、429 限速、5xx->502、非 JSON content 502、无 sentences 502、缺 text 400、超 MAX_TEXT(20000) 截断、外裹文字抽 {...} 回退、空 en+空 zh 丢弃 + 非字符串规整、DS fetch 抛错 502、缺 SUPABASE_URL 500、content 非字符串 502。

**测试 3：真浏览器 chromium e2e（22/22 PASS）** — outputs/v2_e2e.js。复用 M4 真浏览器管线（memory: real-browser-testing-arm64），http server 直接读 repo + 特殊路径 test.pdf + worker 注 polyfill，puppeteer mock translate-proxy（按 \n 切句返 【译N】+原句）+ supabase REST（含 OPTIONS 预检 CORS）+ 预置 sb_session。覆盖：T1 阅读器开+textLayer 渲染（3 span，live="Hello world.This is a test.Fig. 3 shows the result."）；T2 开关->预译 2 页->"译文就绪 2 页"+proxyCalls>=2；T3 page1 tr-sent 叠层 3 个+aria-pressed=true+data-pair；T4 点句弹 ZH 浮层（【译1】Hello world.）+tr-active；T5 M4 共存（有选区时点句不弹浮层）；T6 关阅读器 readerState=null；T7 重开缓存命中零调用+叠层重铺；T8 离线 toast"翻译需联网"；T9 清缓存+401->toast"key 无效"+page1 标 error+停预译 page2 未译（sbFetch 401 刷新重试一次，proxyCalls=2）。截图 haiku 视觉 QA：框架齐全（阅读器/翻译按钮/状态/叠层/译文气泡），3/3 句全匹配（programmatic 核实 trSentCount=3 matched=3），无视觉异常。**无 pageerror**。

**测试基建坑（非 app bug，已存 memory feedback_real_browser_testing 坑 6-8）**：沙箱 chromium 130（2024-10）缺 `Uint8Array.prototype.toHex`（Chrome 133+/Safari 18.2+ 才有），pdf.js v6 worker 用它解析任何 PDF 抛 `n.toHex is not a function`。生产目标浏览器（iOS Safari 18.7+、现代 Chrome）原生有，不影响。修法：http server 对 `/vendor/pdfjs/pdf.worker.min.mjs` 返回 polyfill+真代码（setRequestInterception 拦不到模块 worker 抓取，必须 server 层注）；主线程 evaluateOnNewDocument 兜底。另：pdf.js fake worker（delete window.Worker）走 `import(workerSrc)` 按导入模块 URL 解析相对路径->404，不能用，保留真 worker。

**结论：v2 代码无 bug**（三层测试全绿，55+48+22=125 条断言）。**唯一阻塞：后端未部署**--translate-proxy Edge Function 404（未 deploy）、translations + user_secrets 两表 PGRST205（0003_translate.sql 未跑）。部署步骤见交付。（⚠️ 此为验证时状态；后端已于 2026-08-06 部署并跑通，见下节「v2 翻译上线 + 两 bug 修复」）

**未自测（须用户）**：(1) Supabase Dashboard 跑 0003_translate.sql + deploy translate-proxy；(2) 真实 DeepSeek key 端到端（mock 已验证全链路，真 key 仅多一层 DeepSeek 返回 JSON 是否被 proxy 正确规整--沙箱测 2 已覆盖结构异常分支，但真 DeepSeek 返回形态建议实测一次）。

## v2 翻译上线 + 两 bug 修复（2026-08-06）

**后端已部署（更正上节"后端未部署"）**：用户在 Supabase Dashboard 跑了 0003_translate.sql（translations + user_secrets 两表）+ deploy translate-proxy Edge Function，并配真实 DeepSeek key 端到端跑通--能看到译文下划线即证明整链路（Edge Function -> DeepSeek -> translations 表 -> IndexedDB 缓存 -> textLayer 叠层）工作。上节"唯一阻塞：后端未部署"已消除。

**真机测试发现两 bug（Mac 浏览器）**：用户用真实论文测翻译，反馈"看得到下划线，但不是每个文字下都有下划线，感觉划歪了"。诊断出两个复合 bug：

- **Bug A：tr-sent 下划线错位（CSS 继承）**。根因：`.textLayer span`（pdf.js 文字层定位规则）设 `position:absolute`，而 `tr-sent` 是 `<span>`（非 `<mark>`），继承 absolute 后脱离 inline 流，下划线 `border-bottom` 不贴文字基线而漂到 span 定位框，视觉上"划歪/部分文字没下划线"。`mark.hl` 用 `<mark>` 有专门规则不受影响。修：`.textLayer span.tr-sent` 加 `position:static`（覆盖继承的 absolute，回 inline，同 mark.hl），下划线对齐正确文字。

- **Bug B：跨行句子匹配失败 6/8（行界空白错配）**。根因：textLayer 的 `map.raw` 是各 span 文本节点直接拼接，**行界无空格**（pdf.js 行末 span 文本不含尾空格，下一行 span 直接接上，如 "APIcost"）；而 DeepSeek 返回的 en 句子行界**带空格**（"API cost"）。trFindOffset 三级匹配（原样 / 去\n / 归一化空白）都要求 en 空白形态与 raw 一致，行界错配时 6/8 句匹配失败 -> 未包 span -> 无下划线。修：trBuildTextMap 增构 `noWs`（raw 去全部空白）+ `noWsToRaw`（每个 noWs 字符对应的 raw 偏移）；trFindOffset 加 level-4 兜底--去全部空白后在 noWs 匹配，再经 noWsToRaw 映射回 raw 偏移区间。去空白匹配放最末兜底（前 3 级仍优先，保偏移精度）。

**验证（Node，抽真代码）**：outputs/verify-fix.js 从 index.html 花括号匹配抽取真实 trBuildTextMap + trFindOffset，mock document.createTreeWalker（含 currentNode）+ readerState，跑测试 PDF 的 9 行 textLayer + DeepSeek 返回的 8 句：3 级匹配 2/8 -> +level4 8/8，且 8 句偏移区间在 raw 里去空白后与 en 一致（偏移内容一致性 8/8）。`node --check` index.html 主脚本 + sw.js 语法 OK。

**测试 PDF**：验证/v2-translate/test-translate.pdf（pdf-lib 生成单页 US Letter，8 句含 Fig.3 / et al. / 0.01-0.12 / 跨行长句 / 分号，专测切句与偏移匹配，省 DeepSeek API 钱）。

commit 923fd43，sw.js CACHE v22->v23。已 push GitHub Pages（用户 Mac SSH 推送）。

**未自测（须真机）**：硬刷新线上站后用 test-translate.pdf 重测--8 句每句下都有对齐下划线 + 可点 + 弹中文。

## v2 翻译 P0+P1 修复（5-agent 对抗性审查，2026-08-06）

用户报 5 问题：(1)长论文漏句 (2)句内下划线断续 (3)句子划线不准 (4)跨页上下文缺失 (5)**最影响体验**：每次开阅读器都弹"有新版本"，点刷新后无法正常使用。用户驳回初版诊断（"可能不太是这样"），要求开 5 个 fable 子 agent 对抗性审查。审查证明初版多处误判，收敛出真实根因：

- **漏句(15-20%)**：DeepSeek 字符级 verbatim 漂移--断词连字符(knowl-\nedge→knowledge)、弯直引号(‘→')、dash 变体(–→-)，level-1~4 都匹不上。
- **句内断续**：tr-sent 跨多个 textLayer span（TreeWalker 分段），每段单独 border-bottom，段间断开是结构性的（非 bug），纯散文无 gap 可用 overlay 改善（本次未做，留后续）。
- **划线不准**：偏移区间端点本身精确，真缺陷是 level-3 缺 fromPos（页内重复文本匹到更早位置 + pos 倒退连锁）。
- **SW 每次提示新版本**：初版误判"SW 没真更新"--错，每次部署确有新 SW。真因：(a) 旧注册逻辑每次 updatefound 都提示（含首次安装）；(b) 旧 fetch 用 cache-first 导航（永久卡旧版，reload 后仍旧版，再开又提示）；(c) 无 waiting 模式，install 即 skipWaiting 在阅读中途强制 reload 丢现场。

**P0 SW 修复（commit 2c505b3）**：sw.js 改标准 waiting 模式（install 不 skipWaiting，等页面 postMessage SKIP_WAITING 才接管）；fetch 导航改 network-first（总拿新版，离线回缓存）；资源 cache-first 加 res.ok 守卫（不缓存 404）；横幅改底部 pill（z-90 不挡顶栏）+ "稍后"关闭 + sessionStorage 同会话不重复弹；updatefound 仅新 SW installed 且已有 controller 才提示；controllerchange 才 reload。CACHE v23→v24。index.html SW 注册块配套重写。

**P1 匹配修复（commit 4fc684c）**：
- **P1-a**：trFindOffset level-3 补 fromPos（换算 norm 下标）；trApplyPage 加 pos 单调护栏（found.start>=pos 才用，防倒退）。
- **P1-b**：trBuildTextMap 增构 fold/foldToRaw（断词连字符 + 弯引号/dash 归一 + 去空白）；trFindOffset 加 level-5 兜底（折叠后在 fold 串匹配，经 foldToRaw 映射回 raw 偏移）。trFoldStr 与 fold 构造严格一致（dash 归一→去空白→词中 hyphen 去除 同序）。**坑**：断词连字符检测初版用 `-\n` 失效--textLayer 跨行 textNode 拼接后 raw 是 "knowl-edge" 连写无 \n；改用"归一后 hyphen + fold 串前一字符 + raw 下一非空白字符均字母"检测，断词/正常 hyphen 统一处理。
- **P1-c**：source_hash 改 sha256(TR_CACHE_VERSION + text)，TR_CACHE_VERSION 初始空串（等价 sha256(text)，兼容既有缓存），改 Edge Function 的 prompt/model 时 bump 即失效旧缓存重译。
- **P1-d**：trApplyPage 已算 pg.matched；trRenderStatus 整篇就绪时附"本页 N/M"+漏句时"点此重译"；状态栏可点触发 trRetranslatePage（绕过缓存命中检查，强制重调代理覆盖）；滚动刷新状态。

**验证（Node 抽真代码）**：outputs/verify-p1b.js 花括号匹配抽取真实 trBuildTextMap/trFoldStr/trFindOffset，mock createTreeWalker，5 场景全过：A 回归 8/8；B 弯引号匹中；C dash 变体匹中；D 断词连字符匹中；E pos 单调（重复文本 3 次严格递增）。P1-c sha256(''+text)===sha256(text) 兼容 + bump 失效。主脚本 + sw.js 语法通过。SW 注册块 + sw.js 静态审查（waiting/network-first/res.ok 守卫都对）。真浏览器 e2e 未做--outputs chromium 资产跨会话清空（重搭需下 204MB + 处理 8 坑，成本不合理），核心匹配逻辑已由 Node 抽真代码覆盖，SW 是标准模式。

**未自测（须真机）**：硬刷新线上站后用真实长论文测--漏句减少（漏句率应从 15-20% 降）、句内断续仍存在（结构性，本次未修）、SW 不再每次弹（仅真有新版时弹一次，点刷新后正常）。

## v2 SW 真机两 bug + PWA icon 内联（2026-08-06）

002369d（P0+P1）push 后用户 iPhone 真机报两个 SW bug：

- **Bug 1：controllerchange reload 退出阅读器**。SW 接管触发 controllerchange -> `location.reload()`，reload 后页面回首页（阅读现场丢）。修（commit adf0dfc）：reload 前存 `sessionStorage('reader-restore')=paper.id`；startup 检测该项则 `openReader` + `restoreReaderProgress` 恢复页位。
- **Bug 2：controllerchange reload 无限循环**。reload 后新页面仍监听 controllerchange，SW 已新版时再触发 reload，循环。修（commit 6495658）：reload 前另存 `sessionStorage('sw-reloaded')`；controllerchange handler 检测该标记则不 reload（一次性 guard），reload 后清标记。reader-restore 独立不受影响。

6495658 push 后用户确认 SW 正常。

**PWA icon 内联（commit 07352c5）**：参考用户另一 PWA「自控台」做法，`<link rel=apple-touch-icon>` 的 href 从外部 `icons/apple-touch-icon.png` 改为 base64 data URL 内联（980 字符，解码 735 字节 == 原文件，180x180）。iOS/Mac 主屏图标不再依赖外部文件路径或 SW 拦截，子路径部署 icon 404 隐患彻底消除。删原运行时改写 apple-touch-icon 绝对路径的 IIFE（内联后会反向覆盖 data URL）。manifest 仍外部 + sw.js manifest 拦截不变（manifest icons 仍走 SW 改写绝对路径）。Node 验证：data URL 解码字节 == 原文件、PNG 180x180、IIFE 无残留、主脚本语法 OK、manifest 仍外部。**已 push**（07352c5 + acb5153，用户 Mac SSH）。后续用户报 iPhone 仍无图标，根因：iOS 16.4+「添加主屏」用 manifest icons（非 apple-touch-icon），只内联 apple-touch-icon 不够。**manifest 全内联（commit acb5153）**：`<link rel=manifest>` 改 data URL（percent-encoded JSON，3 个 icon src 也内联 base64），manifest 不再外部请求，iOS 子路径 icon 404 隐患彻底消除。已 push，用户确认 iPhone 主屏图标正常。

## v2 翻译 bug1: 切论文残留译文进度（2026-08-06）

用户报：A 篇译完 11/11，切到 B 篇再切回 A，状态栏仍显示旧译文进度（"翻译 3 页"），需点翻译才更新。代码排查：translations store 只 `put`（754）无任何删除函数（dbDelete 585 只对 papers，删 paper 时连 translations 都不删变孤儿），syncPull/syncPush 是 LWW upsert 不删本地行，trExtractPageText 从 PDF 直接提取（不依赖 textLayer DOM，hash 稳定）--**缓存只增不删，不会 11->3 丢页**，"3 页"是状态栏残留（bug1）非缓存丢。

根因：openReader 建新 readerState（tr:null）但**没调 trRenderStatus() 清状态栏 DOM**（残留上篇进度），**没调 trCancel() 取消旧预译 loop**（旧 loop 继续跑、继续 trRenderStatus 干扰新篇）。

修：openReader 加两行--scrollEl 清空后 `trCancel()`（取消上篇 loop，旧 loop 2624 cancelled check break）；readerState 赋新后 `trRenderStatus()`（rs.tr=null -> 状态栏清空）。Node 验证 8/8：trCancel 设 cancelled+enabled=false、readerState=null 安全、trRenderStatus tr=null 清空残留、openReader 切换模拟（A 11/11 -> B 状态栏清空）、旧 loop break。

**待真机测**：切论文状态栏是否立即清空（不再残留旧进度）；"翻译 3 页"现象是否消失（若消失则确认是 bug1 残留；若仍在则另有根因，需进一步查）。

## v2 翻译 bug2: 4-11 重译诊断 + 修 A（2026-08-06）

用户澄清 bug1 的"3 页"**不是显示问题，是第 4 页开始都重新翻译**（1-3 命中缓存，4-11 重译），且**每次重新点翻译都 3/11**（不只首次切回）。推翻 bug1 残留诊断。

**对抗性审查**（3 个 fable agent 独立收敛）：
- 排除 text 提取非确定性（agent 1 用项目 vendor pdf.js Node harness 实测 streamTextContent 跨 getDocument 重载 byte-identical；renderTextLayer 与 trExtractPageText 填的 textContentCache 参数逐字相同）。
- 排除 sync/IDB 丢行（agent 2：单设备 syncPull 只启动/登录触发，切论文不触发；translations 无删除函数；uid=crypto.randomUUID 不冲突；hash 代码逐字未变）。
- 排除 zombie loop（agent 3：与"11/11 译完"矛盾，closeReader 本有 trCancel）。

**根因**：trPretranslateLoop catch 分支（2633）`tr.done = i` 把失败页也计入 done，trRenderStatus 就绪判据 `done>=total`（2544）-> 8 页全错也显示"译文就绪 11 页"假象。4-11 译失败（DeepSeek 限速 429 / 502 / dbPut 写库失败）不 break（非 key/登录错），error 计入 done 假"译完"，没存缓存，每次重译。1-3 成功存了命中。

**修 A**（失败可见）：
- trPretranslateLoop catch（2633）：`tr.done = i` 改 `tr.failed = (tr.failed||0)+1`（失败不计 done）。
- trPretranslateLoop init/结束：加 `tr.failed=0; tr.finished=false`；loop 结束设 `tr.finished=true`。
- trRenderStatus（2542-2545）：加 failed/finished，txt 显示"部分就绪 done/total（失败 N 页）"或"翻译中 done/total（失败 N 页）"，不再假"译完"。
- toggleTranslate：重置 failed/finished。
- 3243 reader-tr-status click 判据不变（tr.done 语义没变，仍是成功数）。

**诊断日志（临时，定位后删）**：trEnsurePairs 记每页 `cached(有/无)/cHash/nHash/match/textLen` + 代理返回 pairs 数 + dbPut 成功；catch 记 error message。用户复现看 console 即可区分：cached 无=没存（代理/dbPut 失败）、cached 有但 match=false=存了不匹配（text 变，回 agent 1 深查）、代理返回 null=代理失败、dbPut 后无日志=dbPut 失败。

Node 验证 10/10：全成功"译文就绪 11 页"、部分失败"部分就绪 3/11（失败 8 页）"、catch 不推进 done、限速不 break。语法 OK。

**待真机测**：push 后复现 A 篇 4-11 重译，看 console 日志 + 状态栏。若 4-11 失败（限速/502/dbPut）-> 据日志修根因（重试/分块/增大 MAX_TEXT）；若 4-11 真译完存了但下次不命中 -> source_hash 不匹配，深查 text 提取。

## v2 翻译 bug2: 504 分块 + 跨页上下文（修 B，2026-08-06）

**真机复现**（push 修 A 后）：状态栏"译文就绪 11 页"但用户观察到有些页译文失败，Console 4 个 translate-proxy 错误（3×504 + 1×546）。

**真实根因**：504/546 是 **Supabase 网关层超时**（Edge Function 代码只返 400/401/429/502，504/546 是网关层、非代码返回）。4-11 页长文本调 DeepSeek 慢，超过 Edge Function 网关超时 -> 504/546 -> trCallProxy `!res.ok` 抛错 -> catch 计 failed、不存缓存 -> 每次重译都 504。1-3 页文本短、DeepSeek 快，成功存缓存。sbFetch 只处理 401（刷新 token），504 原样返回给 trCallProxy 抛错，路径正确。

**次要 bug**：trPretranslateLoop 2637 `tr.done = i`（赋当前页号、非 ++），即使中间失败，后续成功页把 done 推到 11 -> 主文案"译文就绪 11 页"误报（修 A 的 failed 可见，但主文案仍误导）。

**修 B**：
1. **前端分块**（治 504）：加 TR_CHUNK_SIZE=4000 + trSplitText（按段落+句号切，不切断句）+ trCallProxy 重构为 trCallProxySingle（单次调）+ trCallProxy（分块串行调、合并 sentences）。每块小 -> DeepSeek 快 -> 不超时。
2. **跨页上下文**（治跨页句）：加 trGetCtx（取相邻页末尾/开头 1-2 句，限长 600）；trEnsurePairs + trRetranslatePage 翻译页 N 时传 prevCtx（页 N-1 末尾）+ nextCtx（页 N+1 开头）；trCallProxy 首块拼 `<ctx_prev>`、末块拼 `<ctx_next>`；Edge Function SYSTEM_PROMPT 加规则 7：只译 `<page>` 内文本，`<ctx_*>` 仅供跨页句语义不译不输出。DeepSeek 看到跨页句完整语义，两半译文衔接通顺。
3. **tr.done = i 改 tr.done++**（治误报）：done 计成功数，失败不计，有失败时 done<total -> "部分就绪 N/11（失败 X 页）"，不再假"译文就绪"。
4. **bump TR_CACHE_VERSION='ctx1'**：prompt 改（加规则 7），旧缓存 hash 不符自动失效重译（含 1-3 旧译文，统一用新 prompt + ctx）。

**用户取舍**：跨页句选带上下文翻译（非彻底合并+拆回，工程量大暂缓）；漏句 28/30 选分块后观察（分块短文本 verbatim 漂移减少，暂不加 level6 编辑距离模糊匹配）。

Node 验证 21/21：trSplitText（短/空/多段落/单段超长/单句硬切/小 maxChunk）+ trCallProxy ctx 拼接（单块双 ctx/无 ctx/多块首末 ctx/仅 prev/仅 next/跨页完整）+ 取消检查 + pairs 合并。语法 OK。

**待部署+真机测**：Edge Function index.ts 改了 prompt（规则 7），需用户在 Supabase Dashboard 重新部署；前端改动待 push（Mac）。部署+push 后真机测：4-11 应不再 504（分块）、跨页句译文衔接通顺、状态栏准确（无失败"译文就绪"、有失败"部分就绪 N/11（失败 X 页）"）。

## v2 翻译 bug2 续: 546 retry + annotations 400（修 C，2026-08-06）

**真机复现**（修 B push 后 localhost `python3 -m http.server 8080` 测 1f0fa50）：分块**生效**了--Console 出现 `[tr] chunk 1/3 len=1691` 块级日志，p1 分 3 块译完（pairs=33）、p2 分 3 块译完（pairs=39），dbPut 都成功。但 p3/p4/p5 第一块就 546。关键证据：p2 chunk1 `len=2118` 成功、p3 chunk1 `len=2132` 失败--差 14 字符不同结果，说明 546 是 DeepSeek 响应波动叠加长度边界，非纯长度。仍有长度因素：`2132 = chunk 2000 + ctx 标记 132`，ctx 让 body 超 TR_CHUNK_SIZE，落 ~2100 边界偶发超网关时限。另发现 annotations POST 400 `null value in column "note" violates not-null constraint`（saveHighlight 创建高亮 note 默认 null，syncPush 推时违反 Supabase annotations.note NOT NULL）。

**match=false 全重译**（次要，非阻塞）：localhost 算的 nHash 和 Supabase 缓存 cHash 不同（两端 TR_CACHE_VERSION 都是 ctx1，textLen 也一致 p1 都 3906，但 hash 不同 = text 内容微妙差异，PDF 加载方式/版本差异）。导致缓存不命中、触发重译、撞上 546。治 546 后第一次重译成功，localhost 存新 hash，后续 match=true。根因后续查。

**修 C**：
1. **降 TR_CHUNK_SIZE 2000->1500**（治长度边界）：body 含 ctx 标记后 < 1700，远离 ~2100 边界，DeepSeek 响应快不超时。
2. **trCallProxySingle 加 504/546/502 retry 1 次**（治 DeepSeek 偶发波动）：2s backoff 后重试 1 次；401/400/429 不重试直接抛（不可恢复）；fetch 网络层抛错也 retry 1 次；每次 attempt 前检查 cancelled/enabled；200 但格式异常不 retry。retry 只对可恢复的网关超时类，避免无谓重试不可恢复错误。
3. **annotations note null->''**（治 400）：saveHighlight（3285）+ updateHighlight（3301）的 `note: note || null` 改 `note || ''`（新创建不 null）；syncPush 推 annotations 时 `note===null||undefined -> ''` 兜底（治已存在的 null annotation，papers 表不动）。

Node 验证 30/30：retry（200 成功 1 次 / 504·546·502 retry 后成功 2 次 / 504 retry 后仍失败抛 data.error / 401·400·429 不 retry / fetch 网络错 retry / cancelled·enabled=false 抛已取消 / 格式异常不 retry）+ note 兜底（null·undefined->'' / 有值保留 / 空串保留 / papers 不动 note / 删 _dirty）。

**待真机测**：push 后复现 4-11 重译，看 546 是否消失（分块 1500 + retry）+ annotations 400 是否消失（note 兜底）。若 1500 仍偶发 546，再降或加 retry 2 次。

## v2 翻译 bug2 续2: 546 retry 3 次（修 D，2026-08-07）

**真机复现**（修 C 后 localhost 测）：修 C 部分生效--TR_CHUNK_SIZE 1500（chunks 都 < 1700）+ retry 都在跑，且 retry **真的救回两页**（p6 chunk2 546 后 retry 成功整页过、p7 chunk1 546 后 retry 成功整页过）。但 546 仍在 p4/p5 失败。**颠覆性证据**：546 在 chunk `1385`、`1421` 也触发，**根本不是长度问题**（之前以为 ~2100 边界是误判）。546 集中 p4-p7 连续几页、p3/p8 正常，retry 1 次（2s backoff）只救回 50%（p6/p7 过、p4/p5 没过，p4 retry 变 504、p5 retry 还是 546，说明 DeepSeek 还没恢复）。

**546 真实根因**：DeepSeek API 偶发慢/错误 -> Edge Function 等它 -> 超 Supabase 网关时限 -> 546/504。和文本长度无关，是 DeepSeek 那段时间持续波动。retry 1 次 2s 不够覆盖故障窗口。WebSearch 查 546 无果（非标准码），结论基于日志实证。

**修 D**：trCallProxySingle retry 1 次 -> 3 次，backoff 递增 2s/4s/6s（总 12s 覆盖 DeepSeek 故障窗口，等 DeepSeek 恢复）。fetch 网络层抛错也 retry 3 次（递增 backoff）；401/400/429 不 retry（不可恢复）；200 格式异常不 retry；每次 attempt 前检查 cancelled/enabled。日志打印 `[tr] chunk 状态 N，Xs 后重试 (n/3)`。

**ui.js contentWindow 错误**（ sideline）：`Uncaught TypeError: Cannot read properties of null (reading 'contentWindow') at setReferences (ui.js:1:378)` -- grep 确认 index.html 无 setReferences/contentWindow/ui.js、无外部 script src（全内联），**是浏览器扩展注入的脚本**，非 app 代码，忽略。

Node 验证 39/39：retry 3 次（504 三次重试后第4次成功 4 次 / 三次仍失败抛 data.error / backoff 2/4/6 递增 / 504 两次重试后成功 3 次 backoff 2/4 / 网络错 3 次后成功 4 次 / 网络错 4 次仍抛）+ 401/400/429 不 retry 1 次 + cancelled/enabled=false + 格式异常不 retry + note 兜底（null/undefined->''、有值保留、papers 不动）。

**待真机测**：retry 3 次后 p4/p5 类似情况能否救回（DeepSeek 故障窗口 < 12s 应能覆盖）。若仍偶发失败，再考虑改 Edge Function 内部 DeepSeek fetch timeout（AbortController，超时返 502 < 网关时限，根治网关 546）。

## 关键文件

- `计划书.md`：开发唯一依据
- `harness_for_all.md`：跨项目协作规范
- `工作记录.md`：本文件
- `验证/m0-cors-test.html`：M0 CORS 实测页（临时验证产物）
- `验证/m0-supabase-test.html`：M0 Supabase 连通性测试页（输入框填凭据，无 secret 入库）
- `验证/v2-translate/test-translate.pdf`：v2 翻译功能单页测试 PDF（8 句测切句与偏移匹配，省 API 钱）
- `index.html`：M1 app 主体 + M2 阅读器 + M3 导入 + M4 笔记高亮 + M5 日推 + M6 同步（三 tab 含今日推荐 + CRUD + IndexedDB v5 + pdf.js 阅读器 + textLayer 高亮批注 + arXiv/本地/URL 导入 + 订阅/今日推荐/想读/跳过 + Supabase 登录 + 四表 LWW 同步 + Storage PDF，单文件）
- `manifest.webmanifest` + `sw.js` + `icons/`：M1 PWA 壳
- `vendor/pdfjs/`：M2 vendored pdf.js v6（pdf.min.mjs + worker + wasm + standard_fonts）
- `supabase/functions/arxiv-proxy/index.ts` + `supabase/config.toml`：M3 arXiv API 代理（Edge Function，已部署 2026-08-04）
- `supabase/migrations/0001_init.sql` + `0002_fix_schema.sql`：M6 建表 + RLS + schema 对齐（papers/annotations/subscriptions/dismissed 四表 + storage bucket pdfs + annotations text/start_offset/end_offset + subscriptions created_at + 删 touch_updated_at trigger）

## 待办 / 阻塞

- M0、M1、M2、M3、M4、M5 完成（M4 实机 39/39 + Bug A/B/C；M5 实机 17/17 + C1-C5 全绿 + 真 arXiv 端到端 19/19 部署验收），无阻塞。
- **（M4 收尾，须真 iPhone）**：iOS 长按选词/选区手柄、捏合缩放手感、Retina 像素渲染、PWA 添加主屏 + 离线启动——沙箱测不了，留 M7/M8。
- **（M3+M5 收尾）**：✅ 已完成。用户于 2026-08-04 部署 arxiv-proxy（含 C1 的 search_query 扩展），沙箱真浏览器真 arXiv 端到端 19/19 验收通过（M3 导入 + M5 日推，详见上节实机验证）。
- ✅ **M6 完成**（2026-08-04）：建表+RLS / 邮箱密码登录 / 四表双向同步（LWW + `_dirty` + tombstone）/ 本地 PDF 迁 Storage。C5 全绿（unit 27/27 + 真浏览器 mock 21/21 + 真实 Supabase e2e 22/22 + haiku 视觉）。注：之前 M0 笔记"调试 /rest/v1/ 根需 secret key（publishable 不行）"系误判，实测 publishable key 配合 RLS 可直接用（§5.2 已改原生 fetch + publishable key）。
- ✅ **M7 完成**（2026-08-05）：深色主题「墨纸」+ 烫金书页图标 + SW 更新提示 + 捏合缩放（commit 2998a22/1fcdfd5/bc686fb）+ 捏合平移/选区整页修复 + iOS textLayer 命中修复（tabIndex=0 + 删 callout:none + touch passive:true + touch-action）+ 第六轮 fable 对抗审查移植官方 Safari endOfContent 重定位 JS + reset 监听 + touchend passive + 删 -webkit-overflow-scrolling + captureSelection 端点守卫 + hl-menu y 夹取（沙箱 27/27）。**iOS 选词根因定位修复**（commit 35fbe29）：v17 加 renderTextLayer 阶段 trace + unhandledrejection 捕获，iOS 诊断显示 `lastTrace.steps=[factor, OUTER:TypeError:undefined is not a function (near '...t of e...')]`，定位到 `await page.getTextContent` 抛错；读 pdf.js v6 源码确认 getTextContent 内部 `for await(const t of streamTextContent(...))` 依赖 ReadableStream 原生 Symbol.asyncIterator，iOS Safari 18.7 JavaScriptCore 不支持（抛 TypeError），chromium 支持所以 27/27 PASS 掩盖 8 轮。修复：改用 `page.streamTextContent` + `aggregateTextContentStream`（getReader/read 手动聚合，不依赖 async iteration）。v18 实机验证 iOS span 渲染 + 选词高亮正常。诊断面板已移除（commit 071b017，sw v19）。沙箱 27/27。
- ✅ **M8 完成**（2026-08-05）：仓库已建（SSH 推送）+ v1.0 annotated tag + .nojekyll + 子路径审计通过 + 上线 GitHub Pages。两 bug 修复（本地 PDF 保存后添加页不关 / iOS 子路径 PWA 图标缺失）沙箱 chromium 验证 SW manifest 拦截 2/2 PASS + **用户 iPhone 真机验证通过**（图标/保存/PDF/textLayer/今日推荐）。应用户要求把修复 amend 进 v1.0 单 commit `96b9ef9`（原 `8fa264a` 已并入，强推 main + tag），v1.0 = 含修复的干净单 commit。M0-M8 全里程碑完成。

## 选词偏移修复（--scale-factor 变量名，2026-08-11）

**用户报告**：双击选词蓝色选中区有时跟单词偏移（但复制出来是正确的词）。测试 PDF：BF00337259.pdf（Amari 1977，11 页双栏，可删）。

**排查**：
1. jsdom 14 边界用例验证 captureSelection（Range.toString().length 算 offset）+ wrapTextRange（TreeWalker 按 offset 包裹）逻辑全过--app 选词/高亮算法本身没问题。
2. 提取 BF00337259.pdf 第1页 textContent：items 顺序按栏分组（左栏全 + 右栏全），全文通顺--不是顺序问题。发现 PDF 文本层固有瑕疵（非 app bug）："patterns o f excitation" 里 "of" 被存成 "o f"（普通空格 charCode 32，1977 排版硬编码进 PDF 内容流，pdftotext 也这样）、"Cybernetics27" 缺空格、连字符断词 "homo-geneous"--这些影响浏览器原生双击分词，但不是用户报的"选中区偏移"。
3. 真浏览器（puppeteer + tr-audit/chromium）渲染第1页 textLayer，检查 span computed style：**全部 188 个 span fontSize=16px**（浏览器默认回退），而 PDF 实际字号正文 9.84px/标题 23.04px。

**根因**：读 pdf.js v6 源码（pdf.min.mjs TextLayer.#Qi 方法）确认：pdf.js v6 给每个 span 设 inline `left/top/fontSize = calc(var(--scale-factor)*Npx)`，`--scale-factor` 是**宿主职责**（官方 viewer 由 PDFPageView 设；bare TextLayer API 不设，grep pdf.min.mjs 无 setProperty("--scale-factor")）。app renderTextLayer 误设 `--total-scale-factor`（pdf.js v6 不读此变量，grep 确认官方 CSS/JS 全无此变量名）--**变量名错**。`--scale-factor` 未设 -> inline calc invalid at computed-value time -> fontSize 回退 initial 16px -> span 文字宽度与 canvas 字形不匹配 -> 双击蓝色选中区偏移（复制仍正确，因 left/top 定位大致对、span 文本对）。

实测对比（BF00337259.pdf 第1页，scale=1）：
- 修复前（--total-scale-factor）：fontDist {"16px":188}，"Biol." spanW=28.2px vs PDF 字形 item.width=17.3px（**+63% 偏移**）
- 修复后（--scale-factor）：fontDist {"9.84px":182,"23.04px":2,"13.68px":1,"9.6px":3}，"Biol." spanW=17.4px ≈ 17.3px（**对齐**）

**修复**（index.html）：
1. renderTextLayer（2269 行）：`container.style.setProperty('--total-scale-factor', cssScale)` -> `--scale-factor` + 注释更新（说明 v6 inline calc 依赖 + 宿主职责 + 实测数据）。
2. 清理 textLayer CSS 死代码（基于旧版 pdf.js 的错误理解，v6 不用且被 inline style 覆盖）：删 `.textLayer` 的 `--min-font-size/--text-scale-factor/--min-font-size-inv`，删叶子 span 的 `--font-height/font-size(calc)/--scale-x/--rotate/transform`，只留官方 `z-index:1`。保留 `--scale-round-x/y`（官方 setLayerDimensions 用 round() 降采样，fallback 1px，显式设同值）。同步更新 191-195/221-224/2900/3055 处过时注释（`--font-height/--scale-x` -> inline font-size/transform）。grep 确认无代码遗留引用这些变量。

**验证**：puppeteer + chromium + BF00337259.pdf 真浏览器，mode=scale（修复）vs mode=total（现状）对比：fontSize 16px -> 9.84px（正文）/23.04px（标题），span 宽度 28.2 -> 17.4 ≈ PDF 字形 17.3，偏移消除。app 完整 CSS + 清理后 CSS 两种均验证（证明死代码不干扰）。沙箱 tr-audit/chromium（Playwright arm64 chromium 130）+ toHex polyfill（worker.poly.mjs，memory 坑6）。

**待真机测**（注：bf24844 方向已被 ac84c11 纠正，纠正版真机验证通过，见「选词偏移纠正」节）。

## v2 翻译 句内下划线断续修复（overlay，2026-08-11）

**用户报告**：BF00337259.pdf 开翻译后，一句内的下划线不连续（每词一段、词间空白）。

**诊断**（沙箱真浏览器 chromium 131 arm64 + BF00337259.pdf + mock translate-proxy 按句号切句，outputs/check-underline.js）：
- 第1页 38 句匹配（38/40），tr-sent 片段 306 个，29 个多片段句组全跨行。
- 同行片段间 gap>0.3px 断口 171 个，gap 6-12px（如 10.51/8.6/12.61）。
- 根因：pdf.js v6 TextLayer 按 PDF 内容流 text-show 粒度把同行文本拆成**每词一 span**（空格也独立 span w=4.45），`trWrapSentence` 每个 textNode 包一个 tr-sent 片段，每片段独立 `border-bottom`，词间定位 gap 无下划线 => 句内断续。haiku zoom 截图确认同行两段线中间空白缺口约 1-2 字符宽。
- 印证 P1 审查"段间断开是结构性"判断，但当时记"纯散文无 gap"不准--实测有 6-12px gap（pdf.js 每词一 span），故 overlay 改善必要。

**修复**（index.html，+65/-5）：
- CSS（244/250）：`.textLayer span.tr-sent` 删 `border-bottom`；新增 `.tr-underline-layer`（绝对定位 inset:0 pointer-events:none）+ `.tr-underline`（1px 高 bg `--jade-line`）+ `.tr-underline.tr-active`（bg `--jade`）。
- JS 新增 `trPaintUnderlines(pageEl)`：按 data-pair 分组 tr-sent 片段，按 y 聚类成行（容差 3px），每行画一条底线 div（left=minX, width=maxEnd−minX, top=行底−1px），append 到 layer；layer `insertBefore(endOfContent)` 保持 endOfContent=lastChild（M7 第六轮 reset 依赖；reset 的 `container.append(endOfContent)` 也会自动维持顺序）。
- `trApplyPage` 末尾调 `trPaintUnderlines`；`trUnwrapPage` 加删 layer；`trHidePopup`/`trShowPopup` 的 tr-active 同时操作 `.tr-underline`。

**验证**（沙箱真浏览器 check-overlay.js）：
- layer 存在，108 条底线（38 句 × ~2.8 行）；**同行多条底线=0**（每句每行一条连续）；tr-sent border-bottom-width=0px；endOfContent=lastChild=true + layer 在 endOf 前；点句 popup 弹译文 + 底线/span 双 tr-active；**无 JS 错误**。
- 对比：tr-sent 片段同行 gap 仍在 171（结构不变，视觉由 overlay 覆盖）。
- haiku 视觉确认：新全页每行连续长线（旧 border-bottom 方案同行虚线状密集断口），zoom 两段线各自内部连续无词间断口，无视觉异常（底线对齐文字/无跨行错位/颜色对/长度合适）。

**已真机验证通过**（2026-08-11）：iPhone 真机确认句内下划线连续 + overlay 不影响 iOS 选词 hit-test。

## v2 翻译 译文孤儿泄漏修复（删论文级联软删 + 孤儿清理，2026-08-11）

**用户问**：翻译内容会不会一直留在 Supabase，久而久之存不下。

**诊断**（读 index.html 同步引擎 + translations store）：
- 存储：translations 表逐页一行，同 paper+page 复用 id 覆盖（`(cached.id)||uid()`），重译 / `TR_CACHE_VERSION` bump 都是覆盖同 id，不翻倍。容量估算每页 ~10-12KB，1000 篇 20 页论文 ~200MB，Supabase 免费层 500MB 够几千篇--容量近期无危机。
- **真泄漏**：`del()`（删论文）级联删了 PDF 文件 + Storage 对象 + 批注（`dbDeleteAnnosByPaper`），**唯独漏了 translations**。删掉的论文译文变孤儿永久残留（本地 + Supabase + 多设备同步传播）。无 TTL / 自动清理。
- 归档（`changeStatus` -> `dbPut`）走 dbPut 不碰 dbDelete，译文安全（用户明确要求归档不删译文）。

**修复**（index.html，+44）：
- 新增 `dbDeleteTranslationsByPaper(paperId)`：单事务批量软删该 paper 全部译文行（含同 page 多设备多行不去重），末尾一次 `schedulePush` 传播。风格照 `dbDeleteAnnosByPaper`。
- `del()` 在 `dbDeleteAnnosByPaper` 后加 `dbDeleteTranslationsByPaper(p.id)`，补齐级联。
- 新增 `trPurgeOrphans()`：扫 translations，`paper_id` 不在 live papers（已软删或不存在）的行软删。在 `syncPull` 末尾调（此时本地 papers 已最新），清存量孤儿 + 传播。

**验证**（沙箱真浏览器 test-tr-cleanup.js，7/7 通过）：删论文 A -> tA1/tA2 软删 + _dirty；归档 B（changeStatus done）-> tB1 未删；注入孤儿 tOrph（paper_id=pGhost）-> trPurgeOrphans 软删，purged=1；无 JS 错误。

**已真机验证通过**（2026-08-11）：iPhone 真机确认删论文后该论文译文在另一设备 syncPull 后消失（tombstone 传播）。

## 选词偏移纠正（bf24844 误读修正，2026-08-11）

**背景**：bf24844（今天早些 push）修选词偏移时误读 pdf.js v6 源码，设 `--scale-factor` + 删叶子 span 的 font-size/transform calc 规则。后续会话 grep pdf.min.mjs 核实发现方向错了，工作区留了未提交纠正，本次确认后 commit（ac84c11）。

**源码核实（pdfjs-dist@6.2.108 pdf.min.mjs grep）**：
- `--scale-factor` 源码**零匹配**--bf24844 设的变量名 pdf.js v6.2.108 不读。
- pdf.js 给 span inline 设 `left`/`top`（百分比）/`--font-height`（px）/`font-family`，**不设 fontSize/transform**。bf24844 工作记录称"v6 span inline 直接设 fontSize"是误读--源码 `i.fontSize=o` 的 `i` 是 textContent item 对象（前文 `i={angle:0,canvasWidth:0,hasText:"",hasEOL,fontSize:0}`），不是 span.style（`d`=e.style 才是）。
- `--total-scale-factor` 源码多处引用（setLayerDimensions 算容器尺寸 `round(down, var(--total-scale-factor)*Npx, var(--scale-round-x))`）；`--font-height`/`--scale-x` span inline 设；`--min-font-size` 容器设。

**结论**：span 的 fontSize/transform 必须由宿主 CSS 用 inline 的 `--font-height`/`--scale-x` 算出。bf24844 删 calc + 设无效 `--scale-factor` -> span 无 fontSize 来源 -> 应回退 16px + 无 scaleX。未提交纠正（`--total-scale-factor` + 完整变量链：`--text-scale-factor:calc(var(--total-scale-factor)*var(--min-font-size))` + 叶子 span `font-size:calc(var(--text-scale-factor)*var(--font-height))` + `transform:rotate(var(--rotate)) scaleX(var(--scale-x)) scale(var(--min-font-size-inv))`）与官方 pdf_viewer.css 一致，方向正确。

**9.84px 矛盾**：bf24844 工作记录称"实测修复后 fontSize=9.84px、spanW 17.4≈17.3 对齐"，与源码结论（bf24844 状态应回退 16px）矛盾。最可能：bf24844 实测数据记反（"修复前 --total-scale-factor 时的 9.84px"误记成"修复后"），或实测时 calc 规则未真删干净。按用户决定跳过真浏览器实测（源码 grep 已决定性），直接 commit 纠正，待真机测确认。

**修复**（index.html，+20/-8，commit ac84c11）：renderTextLayer 设 `--total-scale-factor`（bf24844 误设的 `--scale-factor` 改回）；`.textLayer` 加 `--min-font-size`/`--text-scale-factor`/`--min-font-size-inv` 派生；叶子 span 加 `--font-height`/`font-size:calc(...)`/`--scale-x`/`--rotate`/`transform` 规则。

**记忆更正**：`feedback_pdfjs_v6_scale_factor.md` 原记 bf24844 错方向（--scale-factor 是宿主职责、--font-height/--scale-x 弃用），已更正为正确变量链 + 误读教训（minified `i.fontSize=o` 的 i 是 item 对象非 span.style）。MEMORY.md 索引行同步。

**已真机验证通过**（2026-08-11）：iPhone 真机确认选词选中区与单词对齐，--total-scale-factor + 完整变量链版本正常。9.84px 矛盾的运行时疑虑消除（纠正版真机对齐正确）。

## 下一步

v1.0 已上线 + iPhone 真机验证通过（2026-08-05）。M0-M8 全里程碑完成。**v2 翻译功能已上线**（2026-08-06）：后端已部署（translate-proxy + 0003_translate.sql）+ 真 DeepSeek key 端到端跑通。两 bug（tr-sent 下划线错位 + 跨行句子匹配失败）已修复并 push GitHub Pages（commit 923fd43，sw v23）。

**v2 翻译 P0+P1 修复（2026-08-06，5-agent 对抗性审查）**：用户报漏句/句内断续/划线不准/SW 每次提示新版本 5 问题，驳回初版诊断后开 5 个 fable 子 agent 对抗性审查，收敛真实根因（见上节）。P0 SW 改标准 waiting 模式 + network-first 导航 + res.ok 守卫 + 底部 pill 横幅（commit 2c505b3，sw v24）。P1 匹配：level-3 补 fromPos + pos 单调护栏、level-5 字符折叠（断词连字符/弯引号/dash）、source_hash 缓存版本化、本页 matched 率可见 + 重译本页（commit 4fc684c）。Node 抽真代码 5 场景全过。**已 push GitHub Pages**（002369d P0+P1 + adf0dfc reader-restore + 6495658 防循环 guard，用户 Mac SSH 推送；6495658 后用户确认 SW 正常）。**07352c5 + acb5153（PWA icon + manifest 全内联）已 push**（用户 Mac SSH），用户确认 iPhone 主屏图标正常。**bug1（commit 008e84b）+ bug2 修 A（commit c01faeb）+ bug2 修 B（分块+跨页上下文，待 commit）待 push**：bug1=openReader 加 trCancel + trRenderStatus（Node 8/8）；bug2 修 A=catch 不推进 done 改计 failed + trRenderStatus 显示失败页 + 临时诊断日志（Node 10/10，c01faeb）；**bug2 修 B=真机复现定位真实根因=504/546 是 Supabase 网关层超时（4-11 长文本调 DeepSeek 慢超时，非 Edge Function 代码返回），治以前端分块（trSplitText <4000 + trCallProxy 串行调合并）+ 跨页上下文（trGetCtx 相邻页 1-2 句 + `<ctx_prev>/<ctx_next>` 标记 + Edge Function prompt 规则 7 只译 `<page>`）+ tr.done=i 改 ++（成功计数治误报）+ bump TR_CACHE_VERSION='ctx1'（Node 21/21）**。**2026-08-11 更新**：上述六 commit（008e84b/c01faeb/8b93d8c/1f0fa50/5625327/297351e 修D）+ 后续 bf24844/c615207/3c63fcc/8889ae2 均已 push origin/main（SW v27）。本次又 commit ac84c11（选词偏移纠正 bf24844 误读，见上节）待 push（Mac）。已真机验证通过（2026-08-11）：选词对齐 / 句内下划线连续 / 删论文译文级联清理三项均正常。
