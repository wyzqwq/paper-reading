# 工作记录

> 项目：个人论文阅读 App（计划书 v1.0）
> 最近更新：2026-08-02

## 项目目标

给自己用的论文阅读 Web App：单文件 `index.html` + 本地部署 pdf.js，托管 GitHub Pages，iPhone Safari"添加到主屏幕"当 PWA，Mac 浏览器同地址可用。后端 Supabase（Postgres + Auth + Storage）做双端同步，无自建服务器。中文界面、深色护眼风。二合一：阅读队列+笔记跟踪器 / 内置 PDF 阅读器。

## 里程碑进度

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M0 风险验证 | 进行中 | arXiv CORS 实测 + Supabase 连通性验证 |
| M1 骨架 | 待开始 | PWA 壳、三 tab 首页、条目 CRUD（本地 IndexedDB） |
| M2 阅读器 | 待开始 | pdf.js 连续滚动渲染、进度记忆 |
| M3 导入 | 待开始 | arXiv 元数据 / 本地 PDF / URL 导入 |
| M4 笔记与高亮 | 待开始 | 条目笔记 + 页内高亮批注 |
| M5 日推 | 待开始 | 订阅、每日拉取、排序、加入想读/跳过 |
| M6 同步 | 待开始 | Supabase 登录 + 双向同步 + Storage |
| M7 打磨 | 待开始 | 深色主题定稿、iOS 调优、缓存策略、更新提示 |
| M8 部署 | 待开始 | GitHub Pages 上线、iPhone 主屏幕验收 |

## M0 当前进展

- [x] git init + `.gitignore`（2026-08-02）
- [x] CORS 实测页 `验证/m0-cors-test.html`（2026-08-02）
- [x] 用户本地跑 CORS 测试页，收集结果（2026-08-02，初步 n=1，见下方实测结果）
- [x] Supabase 连通性测试页 `验证/m0-supabase-test.html`（2026-08-02，输入框填凭据，待用户跑）
- [ ] 记录 M0 结论、commit

### M0 验证项

1. **arXiv PDF CORS（最大风险）**：浏览器能否直接 fetch `https://arxiv.org/pdf/<id>` 并读取字节供 pdf.js 渲染。失败回退候选：(a) 存链接跳系统浏览器阅读，(b) Cloudflare Worker 轻量代理。
2. **arXiv API**：`https://export.arxiv.org/api/query` 拉元数据（M3 导入、M5 日推依赖）。
3. **Supabase 连通性**：用纯 fetch 打 /auth/v1/health 与 /rest/v1/（M0 不装 SDK），验 URL+anon key+RLS。

### M0 CORS 实测结论（2026-08-02，已定）

用户在 Mac Chrome 跑 `验证/m0-cors-test.html`，两次 origin 均为 `file://`（未起 localhost 服务器）。虽 origin 为 file://，但**测试2 对照**使结论仍确定（推理见下）：

- **测试2 arXiv PDF（cors）：PASS**（status 200 / type=cors / 读到 2.16MB `application/pdf`）。从 file:// null 源能读到，说明 arxiv.org PDF 发 `Access-Control-Allow-Origin: *`。**=> PDF 可直连，pdf.js 无需代理。** 计划书 §7 原 PDF CORS 风险解除。
- **测试1 arXiv API（cors）：FAIL**（TypeError: Failed to fetch）。推理：测试2 证明 Chrome 允许 file://→https（前提：服务器发 ACAO:*），故测试1 失败不是 Chrome 的 file:// 策略，而是 export.arxiv.org 不发 ACAO:*。**=> API 跨域被阻，app（github.io）也无法直连，M3/M5 需轻量代理。**
- 测试3（no-cors）：OPAQUE，符合预期。

**为何 file:// 仍可下结论**：测试2 成功 ⟹ 服务器发了 ACAO:* 才能让 null 源读到；测试1 失败 ⟹ 该服务器没发 ACAO:*。两者对照排除了"Chrome file:// 策略"这一混淆因素。localhost 重测非必需（结论已定）；若日后想金标准复核可再跑。

**结论（定稿）**：
- PDF 加载策略 = 直接 fetch，无需代理。
- API 元数据策略 = 需代理（Cloudflare Worker 或 Supabase Edge Function，M3 时定）。

## 关键文件

- `计划书.md`：开发唯一依据
- `harness_for_all.md`：跨项目协作规范
- `工作记录.md`：本文件
- `验证/m0-cors-test.html`：M0 CORS 实测页（临时验证产物）
- `验证/m0-supabase-test.html`：M0 Supabase 连通性测试页（输入框填凭据，无 secret 入库）

## 待办 / 阻塞

- 用户去新建独立 Supabase project（2026-08-02 决定：与原项目隔离）。建好后用户在 `验证/m0-supabase-test.html` 填 URL+anon key 跑连通性验证、回报结果（M0 无需把 key 发我）。
- CORS 实测已完成（见上方结论）。

## 下一步

收到 Supabase 连通性结果后：记录 M0 结论、commit，进入 M1 骨架。API 代理方案（CF Worker / Supabase Edge Function）留到 M3 定。
