# 个人论文阅读 App 计划书

> 版本：v1.0（2026-08-02，grill-me 拷问对齐后的定稿）
> 状态：**待开发**。本文件是开发的唯一依据；后续需求变更先改本文件再动手。

---

## 1. 项目概述

给自己用的论文阅读 Web App：一个 `index.html` 应用本体 + 本地部署的 pdf.js 库，托管在 GitHub Pages 上，iPhone 通过 Safari"添加到主屏幕"当 PWA 用，Mac 浏览器打开同一地址也可用。后端用 Supabase（Postgres + Auth + Storage）做双端数据同步，无自建服务器。

界面语言：中文。视觉风格：深色护眼风（深底浅字、柔和蓝/紫强调色）。

---

## 2. 产品定位

二合一：

1. **阅读队列 + 笔记跟踪器**：管理"想读/在读/已读/弃读"的论文，记录评分、标签、笔记。
2. **内置 PDF 阅读器**：app 内直接阅读 PDF（pdf.js 渲染），支持页内高亮批注。

非目标（明确不做）：

- 批注写回 PDF 文件本体。
- 多用户/社交功能。
- 本地定时推送通知（iOS PWA 无可靠能力，日推改为打开 app 时生成）。
- LLM 打分推荐（留作后续迭代）。

---

## 3. 功能需求

### 3.1 论文导入（三个来源）

| 来源 | 行为 |
|---|---|
| arXiv 链接 / ID | 粘贴后调 arXiv API（`export.arxiv.org/api/query`）自动拉取标题、作者、摘要、分类、PDF 链接 |
| 本地 PDF 文件 | 文件选择器 / 系统分享导入，PDF 二进制存 IndexedDB 并上传 Supabase Storage；标题默认取文件名，可手改 |
| 其它网站 URL | 只存链接，元数据手动填写；阅读时优先尝试在线加载，失败则提示用户手动下载后走"本地导入" |

### 3.2 日推系统

- **订阅配置**：用户维护若干订阅，每条 = 关键词组（支持多组 AND/OR）+ arXiv 分类（如 cs.CL、cs.LG，可多选）。
- **生成时机**：每天首次打开 app 时，调 arXiv API 拉取订阅分类的最新投稿，本地过滤排序，生成"今日推荐"列表。（arXiv 每天只更新一次，无通知推送，打开即刷新。）
- **排序**：基础相关性分（关键词匹配度）+ 个性化加成（与已读/高分论文的关键词重叠度），只调序不过滤。
- **交互**：每条显示标题、作者、摘要（折叠）、命中的关键词高亮、相关性分；操作："加入想读"（自动拉全元数据入库）/ "跳过"（不再出现，跳过头列表本地记录）。

### 3.3 阅读队列

- 三 tab 结构首页：**今日推荐 / 阅读队列 / 已读归档**。
- 阅读队列内按"在读"优先、"想读"按添加时间或手动排序。
- 状态流转：想读 → 在读 → 已读 / 弃读；全部可逆，弃读保留记录。

### 3.4 PDF 阅读器

- pdf.js 渲染，**连续垂直滚动**（整篇如网页），顶部显示当前页码 + 总进度条。
- 记忆每篇最后阅读位置，再次打开自动跳转。
- 双指缩放适配（iOS 手势）。

### 3.5 笔记与批注

- **条目级笔记**：每篇论文一个 Markdown 文本框，随时编辑。
- **页内高亮批注**：阅读器中选中文字 → 高亮（可选颜色）/ 附加批注；数据存在 app 内（Supabase + 本地），**不修改 PDF 本体**；条目页有批注汇总列表，点击可跳回 PDF 对应位置。

### 3.6 缓存与离线

- 元数据全量存本地（IndexedDB），离线可看队列、笔记。
- arXiv 论文的 PDF 默认在线加载；卡片"缓存"按钮**手动缓存到本地**（2026-09-14 变更：原"在读自动缓存"方案弃，改手动——存储量用户自控，免自动清理策略），缓存后阅读时本地优先（离线可读、秒开）。缓存只存本机 IndexedDB，不上传 Storage（守 §3.7）。
- 缓存清理：卡片"删除缓存"按钮按篇删；删除论文时级联清理本地文件。
- Service Worker 缓存 app 本体与 pdf.js，保证壳离线可开。

### 3.7 数据同步（Supabase）

- 唯一后端：Supabase 项目（免费档）。
- 登录：邮箱 magic link / 密码，单用户。
- 同步内容：全部元数据、笔记、高亮批注、订阅配置、日推跳过列表（Postgres 表）；本地导入的 PDF 二进制（Supabase Storage）。
- arXiv 来源的 PDF 不上传 Storage（按需从 arXiv 重拉）。
- 冲突策略：条目级 last-write-wins（按 `updated_at`），笔记/高亮随条目合并。单用户双端场景足够。
- 同步时机：打开 app 自动拉取 + 本地变更后防抖推送。

---

## 4. 数据模型

### 4.1 论文条目（papers）

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | 主键 |
| title | text | 标题 |
| authors | text[] | 作者列表 |
| abstract | text | 摘要（可空） |
| source_type | enum | `arxiv` / `local` / `url` |
| arxiv_id | text | 可空 |
| url | text | 原文链接 |
| local_file_path | text | Supabase Storage 路径（仅 local 来源） |
| status | enum | `want` / `reading` / `done` / `abandoned` |
| rating | int 1-5 | 可空 |
| tags | text[] | |
| notes_md | text | 条目级 Markdown 笔记 |
| read_progress | float | 最后阅读页码/滚动位置 |
| added_at / updated_at | timestamptz | |

### 4.2 高亮批注（annotations）

id、paper_id、page、选区定位信息（pdf.js textLayer 的文本+偏移）、颜色、批注文本、created_at。

### 4.3 订阅（subscriptions）

id、名称、关键词组（jsonb，AND/OR 结构）、arxiv 分类数组、启用开关。

### 4.4 日推跳过（dismissed）

paper arxiv_id + 日期，跳过的推荐不再出现。

---

## 5. 技术架构

### 5.1 仓库结构（草案）

```
/                      GitHub 仓库，GitHub Pages 从根目录或 /docs 发布
├── index.html         应用本体（单文件：结构+样式+逻辑）
├── manifest.webmanifest
├── sw.js              Service Worker（缓存壳 + pdf.js）
├── vendor/pdfjs/      pdf.js 官方构建产物（js + wasm，本地部署不走 CDN）
├── icons/             PWA 图标（apple-touch-icon 等）
└── docs/              计划书、工作记录
```

不依赖任何外部 CDN（国内访问稳定性 + 离线可靠）。

### 5.2 关键技术选型

- **pdf.js**：Mozilla 官方预构建包，vendor 进仓库。
- **Supabase**：原生 fetch + publishable key 直连 REST/Auth API（M0/M3 先例，零新依赖，不 vendor SDK；2026-08-04 M6 确认）。
- **前端框架**：无框架，原生 JS 单文件（个人项目，控制复杂度）。若开发中复杂度失控，再评估是否引入轻量框架——变更需先改计划书。
- **存储**：IndexedDB（原生或极简封装）存元数据缓存与 PDF 缓存。

### 5.3 PWA 要点

- `manifest.webmanifest`：名称、深色主题色、standalone 模式、图标。
- iOS 特殊处理：`apple-touch-icon`、`apple-mobile-web-app-capable`、viewport-fit=cover 适配刘海屏。
- SW 更新策略：检测到新版本时页面顶部提示"有更新，点击刷新"，不静默强刷（防止阅读中断）。

---

## 6. 界面设计

- **风格**：深色护眼。深灰蓝底（非纯黑）、浅灰白正文、柔和蓝/紫单一强调色用于按钮与高亮；圆角卡片式列表。具体色值在开发时给出 2-3 个候选由用户挑选（按用户规范，配色 token 不擅自定）。
- **首页三 tab**：今日推荐 / 阅读队列 / 已读归档。
- **条目详情页**：元数据 + 状态/评分/标签编辑 + 笔记编辑器 + 批注汇总 + "阅读"按钮。
- **阅读器页**：全屏 PDF + 顶部进度条 + 选中文本弹"高亮/批注"菜单。
- **设置页**：订阅管理、缓存清理、Supabase 登录状态、数据导出备份（JSON）。
- **响应式**：iPhone 竖屏为一等公民；Mac 上限制内容最大宽度居中，阅读器利用宽屏。

---

## 7. 风险与验证计划

| 风险 | 影响 | 验证/应对 |
|---|---|---|
| arXiv PDF 端点 CORS | 在线阅读 arXiv PDF | **M0 实测通过**（2026-08-02）：arxiv.org/pdf 发 `Access-Control-Allow-Origin: *`，浏览器可直接 fetch 供 pdf.js 渲染，无需代理 |
| arXiv API 端点 CORS（M0 新发现） | M3 导入 / M5 日推无法在浏览器直拉元数据 | **M0 实测阻断**（2026-08-02）：export.arxiv.org 不发 ACAO，跨域 fetch 抛 TypeError。回退：加轻量代理（Cloudflare Worker 或 Supabase Edge Function，M3 时定） |
| arXiv API 速率限制 | 日推/导入失败 | 请求间隔 ≥3 秒，失败重试 + 本地缓存上次结果 |
| iOS Safari 对 PWA IndexedDB 的驱逐策略 | 缓存 PDF 被系统清掉 | "在读"缓存之外不依赖本地持久；Supabase 才是真相源；提供导出备份 |
| Supabase 免费额度（DB 500MB / Storage 1GB / 流量） | 超限 | 本地 PDF 控制总量；Storage 用量显示在设置页 |
| ~~Supabase JS SDK vendor 体积~~（已解除） | 首屏加载 | M6 改原生 fetch + publishable key，不 vendor SDK（2026-08-04） |
| 单文件 index.html 随功能膨胀难维护 | 开发效率 | 允许内部按 `<script>` 分区组织；失控再评估拆分 |
| pdf.js Mac 非内嵌字体 textLayer 选词微偏 | Mac 上双击选词，选中区与 canvas 字形左右微偏 | **上游通病**（2026-08-14 实测 mozilla 官方 viewer 同 PDF 同样偏，非本 app bug）：canvas 渲染非内嵌字体用 Mac 系统字体，字形 advance 与 PDF 标准字体微差，致 canvas 字形位置与 textLayer span（PDF 坐标定位）微偏；textLayer 侧无法根治（span 位置=PDF 坐标是对的，改了更糟）。接受；内嵌字体 PDF 不受影响；后续若要缓解可试 `useSystemFonts:false`（有风险未试） |

---

## 8. 开发里程碑

1. **M0 风险验证**：arXiv CORS 实测页 + Supabase 项目创建与连通性验证。
2. **M1 骨架**：PWA 壳（manifest/SW/图标）、三 tab 首页、条目 CRUD（仅本地 IndexedDB）。
3. **M2 阅读器**：pdf.js 连续滚动渲染、进度记忆。
4. **M3 导入**：arXiv 元数据拉取、本地 PDF 导入、URL 导入。
5. **M4 笔记与高亮**：条目笔记 + 页内高亮批注与汇总跳转。
6. **M5 日推**：订阅管理、每日拉取、排序、加入想读/跳过。
7. **M6 同步**：Supabase 登录 + 全量双向同步 + Storage 上传。
8. **M7 打磨**：深色主题定稿、iOS 真机体验调优、缓存策略、更新提示。
9. **M8 部署**：GitHub Pages 上线，iPhone 添加主屏幕验收。

每个里程碑完成即 git commit；push 前给用户看 commit 摘要并确认（按用户规范）。

---

## 9. 需要用户配合的事项

1. 注册 Supabase 账号并创建项目（免费），把 Project URL 和 anon key 给我（anon key 设计为前端公开可用，配合 RLS 是安全的）。
2. 提供 GitHub 仓库（新建空仓库即可），Pages 发布需要 push 权限确认。
3. PWA 图标：开发到 M7 时我会给 2-3 个方向让你挑，或你提供图。
4. 深色主题的具体色值候选，同样在 M7 挑选。

---

## 10. 变更管理

- 本计划书为开发依据；任何需求/方案变更先更新本文件，再改代码。
- 开发过程记录维护在项目 `工作记录.md`（开工时创建）。

---

## 11. v2 翻译功能（2026-08-06 grill-me 对齐定稿）

v1.0 上线后启动。已 grill-me 对齐方案，本节为 v2 开发依据；实现中需求/方案变更先改本节再改代码，开发记录续写 `工作记录.md`。

### 11.1 P1 范围与 UX

- **方向**：阅读器正文翻译（不译摘要、不译 feed 卡片）。摘要/feed 译留 P2。
- **UX**：整篇后台预译 + 页面点句展开。进阅读器开"翻译"开关 -> 后台逐页预译全篇 -> textLayer 句子变可点 -> 点一句在该句旁弹 ZH 译文浮层。开关 off 时正常选区高亮，互不冲突。

### 11.2 引擎

- **DeepSeek**（OpenAI 兼容 API）。选 LLM 非翻译 API：结构化逐句输出 `[{en,zh}]`、可指示保留数学公式、学术翻译质量。
- 调用链：浏览器 -> Supabase Edge Function `translate-proxy`（`verify_jwt=true`）-> DeepSeek。Edge Function 在 Supabase/Deno 服务端跑，可达 DeepSeek；浏览器只到 Supabase（M6 已验证可达）。选 DeepSeek 主因：RMB 计费（支付宝/微信）零摩擦、CN-EN 学术翻译强、极便宜（整篇约 ¥0.02-0.05）。

### 11.3 密钥与代理

- 新建 Edge Function `translate-proxy`（Deno，`verify_jwt=true` + CORS `*`，参考 arxiv-proxy 但加 JWT 校验）。`supabase/config.toml` 加 `[functions.translate-proxy] verify_jwt = true`。
- DeepSeek key 存新表 `user_secrets`（`{user_id PK, deepseek_key text, updated_at}`，RLS `user_id=auth.uid()`），用户在 app 设置页填（masked 输入 + 保存 upsert）。
- `translate-proxy` 流程：验 JWT -> 用调用者 JWT + anon key 转发查 PostgREST `user_secrets?select=deepseek_key`（RLS 只读自己的 key，**不用 service role key**）-> 调 DeepSeek -> 返回 `[{en,zh}]`。key 不进前端持久状态、不进 git。

### 11.4 存储与同步

- 新建 `translations` 表（Supabase）+ IndexedDB 镜像，走 M6 同步引擎（LWW + `_dirty` + tombstone）。
- 表结构：`{id uuid, paper_id uuid FK→papers cascade, user_id uuid, scope text('page'), page_num int, pairs jsonb([{en,zh},...]), source_hash text, _dirty bool, deleted_at timestamptz, updated_at timestamptz}`，RLS `user_id=auth.uid()`。
- 逐页缓存（scope=page + page_num），source_hash 存该页 textLayer 文本 hash，文本变（PDF 重导）自动失效重译。跨设备：Mac 译过 push 到 Supabase，iPhone syncPull 拿缓存不再调 DeepSeek（省钱关键）。

### 11.5 预译流程

- 触发：reader 顶栏"翻译"开关 opt-in（读英文论文不花钱）。
- 首次开（无缓存）：启动整篇后台 progressive 预译，page 1 先译好可立刻点，后续页排队；每页一次 DeepSeek 调用，顺序执行（避速率风险）；顶栏显示"翻译中 n/m"。逐页译好即缓存、句子变可点。
- 缓存命中：开关一开立即可点（零调用）。
- 关开关：隐藏译文 + 取消未完成 pending 调用（缓存保留）。关阅读器：取消 pending。
- **ctx 页界标记 + 泄漏过滤（v2.1.4，2026-09-18 定）**：分块请求体本页文本必须用 `<page>...</page>` 包裹（Edge prompt 第 7 条"只译 `<page>` 内文本"，此前前端漏加，模型只能猜页界，deepseek-flash 偶发把 `<ctx_next>`（常是次页图 1 标签堆，约 20 行无句号碎片）也逐条翻进 pairs——幻影对不在本页 textLayer，匹配必失败，状态栏"本页 m/N"虚低但视觉全译，即"31/53 但每句都翻了"）。双保险：① `<page>` 包裹从源头标页界；② 响应侧过滤——每个 chunk 的返回 pairs 对 chunk 正文 fold 串做单调顺序匹配，不在正文流内且在 ctx 串（fold + 去空白双通道；`trFoldStr` 的 `[–-−]` 实为 U+2013–U+2212 区间，会把 • 等符号折成 '-' 再被连字符规则吃掉，故符号碎片靠去空白直比兜底）的判为泄漏丢弃；纯符号/超短平凡碎片在 ctx 出现也丢。漂移/乱序句照旧计入（匹配失败如实反映）。已污染旧缓存页不 bump 版本（避免全量重译花钱），点"点此重译"即净。

- **跨页句结束页完整译文（v2.1.7，2026-09-21 定）**：原 prompt 只让模型知道 ctx 是上下文，跨页切断的句子里结束页那条的 zh 只译本页后半截。Edge prompt 第 7 条改为明确指令：本页开头延续上页的不完整句，该句 zh 要把 `<ctx_prev>` 中该句的开头部分一并译入，让结束页看到整句译文；en 仍只拷 `<page>` 内文字（前端按本页偏移定位不变）。开头页那条半句照旧只译本页部分（前半句在上一页 popup 里可看）。

### 11.6 展示与切句

- 页面点句叠层：LLM 切句 + 逐句翻译返回 `[{en,zh}]`（en 为 verbatim 原句，用于 textLayer 匹配定位；zh 为译文）。按"文本偏移 -> span"锚定（复用 M4 captureSelection/wrapTextRange 的 textLayer 偏移机制），把每句包成可点区，点句弹 ZH 浮层（该句旁）。
- 切句由 LLM 做（不靠 regex，避免 "Fig. 3"/"et al." 误切）。
- 风险：LLM 返回 en 句与 textLayer 文本精确匹配（换行连字符、空白差异）-> verbatim 返回 + 归一化空白匹配；匹配失败的句降级为不可点（不崩）。
- **level5 折叠匹配两处修（v2.1.6，2026-09-19 定）**：DiLA p4 "∆s0:t−1" 句始终译不出（温度 0.3 下确定性漂移）。① `trFoldStr` 的 `/[–-−]/g` 实为 U+2013–U+2212 **区间**，en 侧把 ∆/∑/∏/×/• 全折成 '-'，而 map.fold 构造走 FOLD 表不折——两侧不对称，模型一字不差抄回 ∆ 也可能在 level5 挂；收窄为 `[–−]` 两字符字面量。② 模型看页图常把 ∆(U+2206) 抄成希腊 Δ(U+0394)（视觉相同码位不同），FOLD 未映射 5 级全挂；两侧统一加 Δ→∆、Σ→∑、Π→∏ 形近字归一（FOLD 表 + trFoldStr 对称）。已知残留：模型把公式写成 LaTeX 下标 `s_{0:t-1}`（违反 verbatim 指令）仍不匹配，prompt 已禁、暂不处理。

### 11.7 文本提取

- 用 `page.streamTextContent()` + `getReader().read()` 手动聚合（iOS Safari 18.7 不支持 ReadableStream async iteration，不能用 `getTextContent`/`for await...of`，见 auto-memory）。逐页提取，保留页边界。
- 聚合后 `{items, styles, lang}` 的 items 拼成页全文喂 LLM 切句翻译。

### 11.8 公式 / 符号 / 中英混排（v2.1 读图通道 + KaTeX）

- textLayer 提取的是渲染后文本（Unicode 符号、上下标），非 LaTeX 源码；老式数学字体还有字形映射错乱（∫→~、ζ→Z、+→4-），提取侧无真值可恢复，真值只在渲染像素里。
- **页图读图通道（v2.1，v2.1.1 起每页都传）**：离屏渲染页图（宽 ~1300px 白底 JPEG）随每页首个翻译 chunk 传 Edge Function → DeepSeek（deepseek-flash 支持读图）。prompt 指示公式一律以图像为准重建为 LaTeX（行内 $...$ / 独立 $$...$$），en 仍逐字拷贝文字层原文（偏移定位用）。图片经 DeepSeek 统一缩至 ~1300×1300、每张 ≤1024 token（约 ¥0.002/页），成本忽略不计，不做开关、默认启用。v2.1 初版用启发式（特殊符号/≥2 行含 =）只给数学页传图，实测漏判（LAPA 论文动作空间 8⁴/256⁷ 页：上标在文字层提取成 "84"/"2567" 纯数字，文本侧不可检测），故 v2.1.1 去掉门槛每页都传。
- **译文弹层 KaTeX 渲染**：zh 中 $...$/$$...$$ 段用本地 vendor/katex（~600KB，守 §5 不碰 CDN）渲染成排版数学；文本段仍 textContent（安全）。KaTeX 懒加载（首次遇公式才拉），加载失败回退显示原始 $...$ 文本。
- 中英混排：LLM 跳过已是中文的部分不译。
- **公式块整块合并（v2.1.7，2026-09-21 定）**：DiLA p5 Eq.(4) 这类独立成块的 display equation 在文字层被换行切成十几行碎片（"Ltotal = ..."、"t mt · (cos(zfwd"、"t ) + 1)2" 各自成句），逐行翻译又散又碎。Edge prompt 新增第 3b 条：独立公式块必须整块合并为一条 sentence——en 逐字拷贝整个公式块文字层文本（保留内部换行；多行 en 的匹配靠 trFindOffset L2 去换行级，discrim15 已验 dila p5 Eq.(4) 单 pair 匹配+包裹），zh 依页图重建为一条完整 `$$...$$` LaTeX。配套 TR_CACHE_VERSION bump 'eq1'（旧缓存失效全量重译，几分钱/篇）。
- 模型侧：deepseek-flash（旧名 v4-flash 别名兼容），**显式 thinking disabled**（V4.1-Flash 默认开 thinking 且 effort=high，不关会慢/贵）。
- 已知局限：DeepSeek 服务端统一缩图后极小上下标可能读错（相比乱码猜测已大幅改善）；en 原文侧（选区/定位文本）仍是乱码文本；LLM 偶发重建错公式。

### 11.9 离线与错误

- 离线："翻译"开关禁用 + toast"翻译需联网"；已缓存页译文离线仍可点开（IndexedDB 本地有）。
- 错误：DeepSeek 5xx/网络失败 -> toast + 重试，已成功页缓存保留；401 无效 key -> toast"DeepSeek key 无效，到设置页检查" + 跳设置；429 限速 -> 退避重试 + 状态提示；关阅读器取消 pending。

### 11.10 UI（墨纸主题）

- 新增 token：`--jade #8fb3a8`（旧玉柔青，ZH 译文文字色）、`--jade-bg rgba(143,179,168,0.20)`（点句高亮底色）。
- "翻译"开关按钮用 accent 古铜金（与现有按钮一致）。ZH 浮层背景 surface-2 + border。
- ZH 译文与 EN 原文靠色区分（旧玉 vs 透明/米白），点句时 EN 句加 `--jade-bg` 底、ZH 浮层显旧玉色译文。

### 11.11 技术约束（守 §5）

无框架原生 JS 单文件 index.html、不依赖外部 CDN、pdf.js vendored 本地部署、Supabase 原生 fetch + publishable key + RLS。新增 translate-proxy Edge Function + user_secrets/translations 表守同条。

### 11.12 留 P2

选区单句按需翻译（复用 M4 选区）、feed 卡片译文（配费用控制）、可见页自动预译（IntersectionObserver）、数学密集 textLayer 提取 polish、整页替换双语视图。

### 11.13 查词（P2，2026-08-12 定；2026-08-14 改内置英中）

阅读器顶栏笔记按钮旁加查词按钮，点开顶栏下拉浮层（搜索框 + 结果区），键入单词查。仅按钮+搜索框入口，不做选词直查。

- **英中内置**：ECDICT（skywind3000）筛 BNC 排名 top 4.5 万词（bnc 1-50000，覆盖常用+学术英语），slim 成 `vendor/ecdict/dict.json`（3.14MB，`{word:[phonetic,translation]}`）vendor 进仓库。app 打开时 `dictEnsureLoaded()` 预缓存：先查 IndexedDB `dict` store（v7 新增，keyPath=word），命中灌内存 Map；否则 fetch dict.json -> 批量 put IDB + 灌 Map。查词查内存 Map，零网络、离线可用。
- **弃英英**：原 Free Dictionary API（dictionaryapi.dev）免费但服务间歇不稳（fail to fetch / load failed），2026-08-14 弃。
- **覆盖局限**：BNC 4.5 万词不含技术新词（configurator / tokenize 等 ECDICT 未收），查不到提示"未找到"。
- 不走 DeepSeek（用户要求免费）；不走外部 API（离线可靠）。

### 11.14 句尾图标点译文（P2，2026-08-12 定）

痛点：现点句事件在 textLayer span 上，与浏览器选词选区行为冲突，点不准、易变双击选词。

方案：每句结尾在 overlay 层加小图标（古铜金书页角 SVG），`pointer-events:auto` 独立可点，单击不与选词冲突。点图标调 `trShowPopup` 弹 ZH 浮层（复用现有）。已译且匹配成功的句才显图标。移除点句 click 委托弹译文逻辑（保留点空白隐藏浮层）；`tr-sent` 只留承载下划线 + 高亮，去 `cursor:pointer`。图标定位复用 `trPaintUnderlines` 的 data-pair 分组几何（末行右端）。

**手机/电脑分流（2026-08-14 补）**：手机触屏点 14px 小图标手指难点中，改分流--`matchMedia('(hover: none) and (pointer: coarse)')` 命中（iPhone）时点 `.tr-sent` 句片段弹译文（anchor=点击片段），CSS 隐藏句尾图标；电脑维持点图标。手机点句与长按选词靠 `sel && !sel.isCollapsed` 守卫区分（拖选中让 M4 接管，tap 点句 sel collapsed 才弹），tap vs 长按手势不同，冲突比桌面双击-单击轻。

**弹层切句导航 + 行分组修（v2.1.2，2026-09-18 定）**：
- 弹层底部加导航条 `‹ i/N ›`（28px 触控目标），页内连续切句，跳过没 DOM 锚点的句（匹配失败未包裹的 pair）；目标句不在视口先 `scrollIntoView` 居中。键盘 ←/→ 切句、Esc 关弹层（仅弹层显示时接管，输入框聚焦让路）。可发现性：导航条常驻可见 + 首用 toast 一次（localStorage `tr_nav_hint`）+ 设置页 API 处一行说明。跨页跳转不做（下一页可能未渲染未包裹，弹层找不到锚点）。
- 下划线/图标行分组改**垂直深交叠**判断（原 `|Δy|<3` 把下标/上标当独立行）：交叠量 ≥ 矮矩形高度一半才并——下标矩形与正文行深交叠 → 并入同一行，单条下划线（治「公式下标句画双线」）；图标不再锚到碰巧排最后的下标行（治「图标掉到下标位置」），且 `left` 由 `maxEnd+2` 改 `maxEnd-4` 贴住标点（悬空感没了）。v2.1.2 初版是「任何交叠都并」，行距紧的论文（DiLA）相邻行 rect 有 1-2px 浅交叠被误并成一行、多行句只剩一条线——v2.1.3 收紧为半高阈值。
- 切句导航的 `scrollIntoView` 会触发「滚动即收起弹层」处理器把刚弹的译文收走（真机上读论文中段、目标句在视口外时必现；弹层一收 `trActivePair` 清空，键盘也跟着失效）——v2.1.3 加 `trNavScrolling` 守卫豁免导航滚动（350ms）。
- **弹层锚点统一为整句联合矩形（v2.1.5，2026-09-19 定）**：原定位锚"点击/切到的单个片段"——桌面点句尾图标恰在句末所以没事，但 ‹ › / 键盘切句锚第一个片段，多行句译文落在第一行下方、盖住后半句原文。改为取该 pair 全部 `tr-sent` 片段（+句尾图标）的视口包围盒，译文放整句下方 8px 居中、下方放不下翻整句上方（盖上文不盖本句）；PDF 不可重排，译文块遮住后续句原文是"整句下方"的必然代价（切句/滚动即移走）。

### 11.15 下载 PDF（P2，2026-09-14 定）

条目详情页（编辑 modal）底部按钮区加"下载 PDF"按钮（复用 `btn-secondary` 样式），仅编辑已有条目且可读（`canRead`：local 有文件 / arXiv·URL 有可解析链接）时显示。

- **数据源**（复用 openReader 路径）：local 走 IndexedDB `pdfFiles`，缺失 fallback Supabase Storage 下载；arXiv / URL 走 `resolvePdfUrl` 归一化后在线 fetch。
- **文件名**：论文标题（跟随表单当前值）替换文件系统非法字符 + `.pdf`。
- **保存分流**（能力检测，不判 UA）：`navigator.canShare({files})` 支持走系统分享面板（iOS 存"文件"/AirDrop/分享）；否则 `a[download]` 直接下载（Mac/桌面）。用户取消分享（AbortError）静默。
- **失败**：toast 提示（在线 fetch 失败 / 本地云端均无文件）。

### 11.16 阅读器夜间模式（P2，2026-09-16 定）

阅读器顶栏查词按钮旁加"夜间"按钮，PDF 反色阅读（白底黑字变黑底白字）。

- **实现**：`.reader-page canvas` 加 `filter: invert(1) hue-rotate(180deg)`——invert 反色正文，hue-rotate 尽量还原彩色插图色相（只 invert 插图会全变补色）。只反 canvas 不反 textLayer，高亮批注/选区/译文浮层颜色保持原色。
- **切换**：两态（开/关）。默认**跟随系统** `prefers-color-scheme`（系统切深色实时响应）；顶栏点过即存 localStorage 覆盖、不再跟随；设置页"阅读器夜间模式"小节显示当前态 + "恢复跟随系统"按钮清除覆盖。
- **存储**：设备级偏好存 localStorage（`reader_night`），不同步 Supabase（主题随设备，手机夜间 Mac 日间各自合理）。
- **已知局限**：hue-rotate 只是近似还原，个别插图颜色仍会失真；iOS 上大 canvas CSS filter 可能略降滚动性能（实机验证）。
